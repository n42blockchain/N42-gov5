package tests

const (
	compatibilitySummaryManualSkip = "manual compatibility summary/report; default suite keeps asserted behavior tests"
	compatibilityGapAnalysisSkip   = "manual compatibility gap analysis; default suite keeps asserted behavior tests"
)
