# QS queue (commander's board, 2026-09-20)

Standing goal: the seven-node qs fleet's TPS. Scored metric: the B mean.
Standing best on the SAFE lineage (S26 vote rule and later): **B 124.3k** (35zzzm, n42-r97, sender cache 4M, both adopted switches). Pre-safety reference only: 132.8k (35zzzc, n42-r89), not comparable.
writer + tx-bounded tail + packet window 8). Protocol: QS_AGENT_PROTOCOL.md.

| id | step | binary | prediction | status | result |
|----|------|--------|-----------|--------|--------|
| S1 | 35zzx: sixteen generators x 500 senders -- restore supply (37% occupancy at 35zzt) | n42-r84 | 77 (6bq): occupancy >60%, B > 127.6k, no BAD BLOCK | falsified | falsified (6bu): B mean 47.0k vs 127.6k standing best; occupancy fell to 8.5% mean, not >60%; no BAD BLOCK/divergence, but 23 blocks in a 37 s window of B2 dropped 183,282 candidates to nonceHigh (up to 99.9% of one block) as the sixteen generators ran dry -- the acceptance note's mass-drop trip, so the result cannot be credited as supply-restored. One-line reason: the "restore supply" premise was never tested -- 6bv shows the funded supply was identical (36,000,000 tx/leg) in both rounds; the doubled generator count doubled the aggregate `-target-depth` instead, and the round failed at ~16% of its budget spent, not exhaustion |
| S2 | 35zzw: per-block base-read cache (parallel.BaseCache) | n42-r85 | 78 (6br): follower `proc` -10% on full blocks | falsified | falsified (6bw): B mean 96.1k vs 127.6k standing best (-24.7%), on 35zzt's own eight-generator shape so it is a direct comparison; every B window ran 40-69% slower per block than 35zzt's; occupancy was up (48.9% mean vs 35zzt's 37% steady-state), so fuller blocks are what got slower. The literal `proc` before/after delta could not be checked: n42-r84's full-block `proc` was never captured and its logs had already rotated out by the time this round finished, so BASELINE proc is n/a, not an estimate -- only r85's own number is on record (proc median 988 ms, p90 1131, n=1722 across the B legs, execMs 717 of it). nonceHigh drops 0 and no BAD BLOCK/divergence (acceptance conditions both clean). Ruled falsified on the measured half of the prediction (B mean must rise; it fell), not on the unmeasurable half |
| S3 | why the leader's build fell back (9064 aborts on 23000 txs): a fallback costs the block its parallelism and used to corrupt it | spec first | 79 (6bs) | spec written, cause open | the build's own reader and the executor's per-worker reader disagreed about a deep-nonce sender's account (buildReaderNonce 4500 vs worker-seen state 0); Block-STM cannot tell that miss from a resolvable conflict and burns all 64 waves finding out; prediction 79 not yet tested against a fresh wave-limit exhaustion |
| S3b | do the routine nonceHigh drops (9200/32200, 13500/18500) also come from the reader disagreement, or are they genuine send-ahead | logs only | 80 (6bt) | done | falsified as genuine send-ahead: both blocks are bug-driven, but not the same bug -- 13659302 is the live reader race (S3/pred 79), 13659303 is downstream of the now-fixed c0931aeb corruption (13659302's fallback wiped 17,036 accounts, so 13659303's build reads the same already-emptied state from both readers, which only looks like agreement); no other build in the log (0 of ~991) dropped any candidate to nonceHigh, so "routine" does not describe this log at all |
| S4 | the Prague delegation check reads every recipient (6bp) | not built | not written | candidate | |
| S5 | the leader's write (~0.5 s) off the critical path | not built | not written | candidate | |
| S6 | per-transaction allocation hotspots (receipt, AsMessage, journal dirties, IBS.Reset maps) | not built | not written | candidate | |
| S7 | 35zzx retry: same 16-generator harness, `-target-depth` halved 45000 -> 22500 so the fleet's aggregate in-flight target returns to 360,000 (35zzt's proven number) instead of the doubled 720,000 S1 actually ran at | n42-r84 | 81 (6bv): every B window >=25% occupancy, B mean > 127.6k, no B-leg block drops a double-digit percent of candidates to nonceHigh with fallback:false | falsified | falsified (6bx): B mean 86.2k -- above 35zzx attempt 2's 47.0k (+83%) but 32.4% below the 127.6k standing best. All four B windows land below the 25% occupancy floor (19.4-22.8%, mean 21.1%), including a sharp B1 win2 collapse (17 blocks/60s, blockTime 3.529s vs win1's 0.645s). 20 committed B-leg blocks drop a nonzero nonceHigh share (63,700 candidates total, two ~20s bursts at each leg's flood ramp), six of them at 99.98% (candidates==failed==4491), all fallback:false/lenient:true/waves:1 with no preceding wave-limit exhaustion -- clause 3 fails outright. No BAD BLOCK/divergence/MODE-FAILED/watchdog; 8 view-timeouts inside the B legs cluster with the drop bursts. Halving target-depth alone did not remove 6bv's synchronized funding-gate inrush (all seven nodes' memory jumps ~1.7GB->~9-10GB in under a minute); the funding gate itself is still untouched. One-line reason: all three clauses of prediction 81 fail independently, though the depth-target change is a real partial win over 35zzx attempt 2 |
| S8 | audit: is a block serialized twice on the vote/import path, as n42-rs found (107 ms, 29% of its cycle)? | static audit | none (audit) | done | falsified for the n42-rs mechanism (handover: Duplicate serialization audit, 31a25647): the proposal carries only BlockHash+TxRootHash, the push/gossip encode is shared across peers, tx hashes reuse the cached wire bytes, commit-to-canonical reuses the decoded block. One confirmed second pass remains on the WRITE path: `encodeTxForStorage` re-serializes all 163k txs into the compact storage record although `tx.enc` is cached (35zy: 118 ms of a 213 ms write; halved by the parallel encode in 35zzb). It is after the vote on a follower, so it belongs with S5, not ahead of it |
| S9 | push/gossip race: `rpc_block_push.go` registers `pushInflight` only after its full decode (line 49), so gossip's peek (`validate_blocks.go:47-61`) can miss it and decode the same 26 MB block again | logs only first | not written | candidate | suspected, never observed live; count double decodes per B leg from existing logs before building anything |
| S10 | 35zzy B1 win2 collapse (17 blocks at 3.5 s, node5 block 13659711 dropped 100,700 of 163,000 candidates as nonceLow): where do the 3.5 s go, and how many stale (mined-not-demoted) candidates does the pool hand the builder per block? Commander's ruling after S7: no third round on the sixteen-generator shape -- two rounds (47.0k, 86.2k) both lost to the eight-generator 35zzt (127.6k); the node-side suspect is pool demotion lagging the chain, which no harness change fixes | logs only (urgent: node logs rotate) | 82 (implicit): the collapse is node-side pool/build work, not per-block slowness or consensus latency | done | confirmed (6by): not a steady 3.5 s/block regime -- one 52 s chain-wide stall between blocks 13659710 (21:37:51) and 13659711 (21:38:43), every node agrees on both timestamps. Root: node5's leader turn stalled 51 s inside its own build queue after 4 straight "committed block not executed locally" failures and a "deferred production resumed" catch-up (`miner: work queue wait` grows from a 0.012 ms median to a single 45,265 ms sample -- the single largest grower, `internal/miner/worker.go:472`); surrounding view r1/r2 timing stays normal (4-67 ms), so consensus round-trip itself was not slow. Symptom: 3 view timeouts with doubling backoff (6/12/24 s, views 7356-7358), node5 re-elected leader each time. Downstream: block 13659711's own fill drops 100,700/163,000 (61.8%) candidates as nonceLow -- the only >25%-nonceLow block anywhere in the round, on the only leader affected; nonceLow share is 0% in B1win1, B2win1 and B2win2, 29.9% in B1win2 (window mean). No `txpool reorg phases` fires during the stall or for 11m42s after (last before: node5 21:37:35, demote 211.8 ms; next: node0 21:49:25) -- the pool's demote sweep does not explain the trigger but confirms the pool went uncleared through it. Evidence preserved to `/data/blockchain/wr-logs/r35zzy-keep/` (197 MB, all seven nodes, full B window) before analysis. |
| S11 | the 52 s leader stall (6by): node5 sat between `miner: commitWork begin` and `miner: parallel fill` for 51 s, three view timeouts, the whole of B1 win2. Commander's ruling on S10: the pool-demotion suspicion is falsified (the stale fill is downstream of the stall). Make the next occurrence explain itself: n42-r86 = r84 + per-step timers inside commitWork before the fill + a one-shot goroutine dump when a build has not reached its fill within 3 s. Run on the proven eight-generator 35zzt shape, which also re-measures the standing best with the import phase line | n42-r86 (diagnostic only, no behaviour change) | 82 (6bz): B mean within the 3.6% noise floor of 127.6k; any build stall > 3 s leaves a dump + prefill-phases line naming the step and lock/call it waited on; otherwise the round still yields the n42-r84-lineage import-phase line | ran 2026-09-20 23:51 EDT - 2026-09-21 00:50:35 EDT as 35zzz on n42-r86; confirmed (6ca) | confirmed on all three clauses (6ca): B mean 126,288 vs the 127.6k standing best (-1.0%, inside the 3.6% noise floor) -- (a) confirmed, though this round alone cannot separate "diagnostics cost nothing" from "a small cost hidden by noise" since no round has run r84's exact bits without the diagnostics on this shape. Zero builds crossed the 3 s watchdog anywhere in the round (max prefill `total` 448 ms), so zero `.stacks` dumps exist -- (b) vacuously confirmed, not live-fire exercised (6bz's unit tests remain the only positive evidence the dump path runs). `import_breakdown.py` line obtained on the eight-generator shape with no stall: body 10 / proc 554 (recov 26, exec 255, finalize 145) / write 214 / total 803 ms, n=1896 -- (c) confirmed. Also measured: `miner: prefill phases` steady state (226 lines) is `specTreeReload`-dominated at ordinary magnitudes (median 111.8 ms) and `headerPrepare`-dominated in the 5 heaviest lines (158-380 ms, all still under 3 s); `parallel fill drops` is zero on every node in both B legs (782 `miner: parallel fill` lines, `failed` sums to 0); no BAD BLOCK, no divergence, no MODE-FAILED; largest chain-wide commit gap 2 s (B1, leader node3) / 3 s (B2, leader node0); 2 view timeouts total, both inside each leg's baseFee-decay warmup, not the flood window. Evidence kept in `/data/blockchain/wr-logs/r35zzz-keep/` (486 MB, node0-6-B.log) before analysis |
| S12 | Commander's ruling on S11: prediction 82 confirmed (126.3k, -1.0%, inside the noise floor), so n42-r86 replaces r84 as the base binary and the diagnostics stay on; no > 3 s stall occurred on the eight-generator shape, so the 52 s stall is parked until a dump catches one. READING CORRECTION that reorders the queue: the harness's occupancy reads 50% for a FULL block (the fill cap is half the header gas ceiling; A-leg windows print 50.0%). 35zzz's B windows at 48.7 / 47.6 / 48.9% are ~97% full blocks (8,100,942 txs / 51 blocks = 158.8k of 163k); 35zzt's 37% was 74% full. The fleet is no longer supply-bound on this shape -- it is bound by the 1.18-1.30 s cycle of a full block, of which the follower import is 803 ms. S12: from the kept 35zzz logs, decompose that cycle on the critical path (leader: trigger -> prefill -> fill -> seal -> propose -> write; follower: arrival -> import -> vote; consensus: QC -> next trigger) and name the two largest items that are on it | logs only (wr-logs/r35zzz-keep) | done (6cb) | in-tenure critical path (median, n=79, confirmed within 1% of the measured 799 ms cycle): push(v-1)->QC(v-1) 520 ms (WAIT, consensus vote/QC round-trip -- NOT the follower's full import, since deferred execution keeps 87.4% of commit votes off the current block's own import path, proposal.go:283-320) + QC(v-1)->push(v) 271 ms (CPU, leader's own commit()/state-root computation, worker.go:2153) = 791 ms. Follower import (757-803 ms) routinely outlives the whole cycle by 1-2x and is not the bottleneck in-tenure; the leader's own write (349 ms median) is off the path (persistWait/insertParent medians are 0 per 6ca). Hand-over (46% of full blocks, not tenure=4's 25%, because slower hand-over cycles let backlog refill) runs 1201 ms median, nearly all of the extra cost in the new leader importing/applying the outgoing leader's last block before it can build (build_prefix 621 ms, vs 6 ms in-tenure) | S12c (6cd) splits both remaining gaps with existing lines + a code trace: hand-over's 508 ms is 97% accounted for by real CPU (prefill 142 ms + fillTransactions 351 ms, dispatch/gates ~0 ms), and the reason it gets no speculative head start is that the cross-tenure hint (proposal.go:461-462) is gated on full import while large blocks vote via the deferred (parent-imported-only) path that never sets that flag before the vote is sent; the 134-149 ms held-vote gap is entirely `CheckDeferredBlock`'s own per-transaction execution time (deferred_includable.go:38), not a dispatch delay (t_checked and t_gate land in the same millisecond in all 64 rows) |
| S13 | Commander's ruling on S12/S12b/S12c (6cb-6cd): in-tenure cycle 799 ms = push->QC 520 + QC->push 271 (leader state root); my parent-import pacing hypothesis is falsified (6cc); the hand-over's 508 ms is real work (prefill 142 + fill 351) because the next-tenure leader cannot speculate (proposal.go:461 guard), and the held-vote gap is CheckDeferredBlock's own 134 ms. The number nobody has ruled on is in 6cd's Gap C: a follower's proposal->prepare-vote `Delivery` is 434 ms at the median -- more than half the in-tenure cycle -- while the vote rounds themselves are gated on the k-th follower having the block. S13: is the delivery of a 26 MB block to the quorum-forming follower the critical path? Define `Delivery` from the code, rank arrivals 1st..6th per block, time the leader's push (encode, per-peer send, serial or parallel, chunking, transport security/muxer) and the follower's receive+decode, from existing lines and a code trace | logs + code trace (wr-logs/r35zzz-keep) | done (6ce) | falsified: `Delivery` (434 ms) is ViewStart->ProposalReceived on the FOLLOWER's own clock (a small consensus message, gated on nothing but itself under two-phase voting), not block-body delivery -- the actual body reaches the quorum-forming follower (rank 4 of 6) in a median 43.9 ms, sent concurrently to all peers (blockchain.go:1773-1806), 8.5% of the 520 ms push->QC segment; Round1(129 ms)+Round2(370 ms), matched to the same 79 blocks, close q2s to 95.9% -- confirmed within the 15% bar. 99.4% of a block's txs already had a cached sender on the follower before it arrived |
| S14 | Commander's ruling on S13 (6ce): hypothesis D falsified -- the block reaches the quorum-forming follower 43.9 ms after the push (8.5% of push->QC), the prepare vote needs no body at all, sends are concurrent and snappy-compressed; transport is closed as a lever. What remains is stranger: Round1 takes 129 ms although the 4th follower holds the proposal at 44 ms, and Round2 takes 370 ms although 86.5% of commit votes are not held. Signing and verifying a handful of votes on one host is milliseconds, so ~450 ms of the 799 ms in-tenure cycle is handlers waiting for something -- a lock or a busy event loop, not work the logs name. S14: n42-r87 = r86 + contention diagnostics only: env-gated mutex/block profiling, vote-path stamps (handler entry -> engine lock acquired -> done, for proposal, prepare vote, PrepareQC, commit vote, CommitQC; commit-vote decision immediate/held with reason), and the runner captures mutex + block + CPU profiles from the sitting leader and one follower in each B leg | n42-r87 (diagnostic only, no behaviour change) | 83 (6cf): B mean within the 3.6% noise floor of 126.3k; the stamps + mutex/block/CPU profiles attribute >= 70% of Round1+Round2 (499 ms) to named waits (lock/queue with holder, or named work); if that fails, the stamps still split each round into k-th-vote-arrival vs leader-side QC-formation | done (6cg) | round 35zzza ran clean (n42-r87, ROUND DONE); B mean 125,125 (-0.93% vs 35zzz, -1.98% vs 35zzt, inside 3.6% floor) -- (a) confirmed. Attribution: Round1 (124 ms) is 90% `r1kth` (waiting for the k-th prepare vote's MESSAGE to arrive) + 2% `r1qk` (its own lock+work+aggregate); Round2 (353 ms) is 100% `r2kth`+`r2qk` (349+4). Follower-side processing of both messages is 0-2 ms in every case measured. Only 1.3% of Round1+Round2 is a named lock/queue-with-holder or named CPU work -- (b) falsified on its literal 70% bar. But the stamps DO cleanly split each round into "waiting for votes" vs "leader QC-formation" -- (c) confirmed: followers/the network, not the leader's own aggregation, are the slow half. All three suspects falsified as material: JournalVote has 0 block-profile samples on all 4 nodes; CommitToCanonical/processOutputs is 10-20 ms/20s CPU; BLS verify is real (25-40% of a tiny CPU budget) but not on the k-th vote's own path and an order of magnitude short of 110-349 ms. Profile caveat: all four 20 s captures (leg+150s) landed in the 400 s decay warmup, not the flood -- CPU/mutex/block numbers are empty-block corroboration, not a full-block measurement. One live build-stall (node6, a real 163,000-tx block, 3 s, self-healed with 2 TCs and 2 sibling-suppressions, no BAD BLOCK). Prediction 83 overall: confirmed, via its own (c) fallback. |
| S15 | Commander's ruling on S14 (6cg): prediction 83 holds only through clause (c); the lock suspects are all falsified (JournalVote: zero block-profile samples; e.mu 3-4% of wall time; QC formed 2-4 ms after the k-th vote; followers answer a Proposal or PrepareQC in 2 ms). The wait is message ARRIVAL: the k-th prepare vote reaches the leader 110 ms into Round1 and the k-th commit vote 349 ms into Round2, for messages a follower produces in 2 ms. Consensus messages ride GossipSub; so does the unconditional ~18-26 MB block gossip fallback (blockchain.go:1755-1764) that every node publishes or forwards on the same per-peer pubsub streams just as the vote rounds run, and bc.lock shows the gossiped copy racing the direct push on every block (491-562 ms/20 s). Hypothesis G: the block gossip fallback head-of-line blocks the votes. S15a (logs): do the k-th votes arrive when the gossiped block finishes arriving? S15b (round): n42-r88 = r87 + env switch that skips the block gossip fallback (default unchanged), one variable | n42-r88 | 84 (6ci): mechanism (r2kth 349 -> under 100 ms, r1kth 110 -> under 50 ms, in-tenure cycle 806 -> under 650 ms) separate from throughput (B mean > 130.8k, the 3.6% noise floor over 126.3k; watch for supply binding near 142k) and safety (no BAD BLOCK/divergence, no rise in view timeouts or block-fetch/catch-up events vs 35zzza) | done (6cj) | S15a (6ch): falsified as the dominant mechanism -- r2kth (349 ms, the bigger delay) scales ~42x from empty to full blocks in a shape that matches CheckDeferredBlock (6cd), not a shared gossip queue, while r1kth (110 ms) grows a smaller, real 74 ms that gossip queue-sharing cannot be ruled out for but also cannot be confirmed for (the receive-side gossip-block lines are all log.Debug, zero occurrences in the kept logs). COMMANDER'S RULING on S15a (6ch): the agent's "falsified" is overruled to INCONCLUSIVE. Its alternative (CheckDeferredBlock's 134 ms explains r2kth) contradicts S14's own stamps: only 1.8% of commit votes were held (cvHeld) and followers cast the commit vote 2 ms after the PrepareQC arrives (pqc2cv), so the check is finished before the PrepareQC shows up and cannot be what the vote waits for. The asymmetry it cites (r1kth 2.3x vs r2kth 42x from empty to full) is what G predicts by timing: Round1 runs at 0-124 ms while the fallback is still being compressed and published, Round2 at 124-477 ms while 18-26 MB sits in every per-peer FIFO rpcQueue, which has no size or type priority (rpc_queue.go). The direct coincidence test could not run (receive lines are Debug). S15b, the switch, decides. New lead recorded for later: the direct Rotor stream for consensus messages succeeds only 58-59% of the time S15b (6cj): switch confirmed fired 7/7 nodes both legs (startup line x5/node, bc.lock gossip-race delay 491-562 -> 163-170 ms/20s, block-topic validator CPU -> 0). Result: r1kth's entire size-dependent growth is fixed (129 -> 63 ms full-block, 55 -> 55 ms empty, a 93% cut) -- hypothesis G confirmed for Round1. r2kth is completely unmoved (333 -> 336 ms full-block) -- hypothesis G definitively falsified for Round2, which remains unexplained by any suspect this campaign has named. In-tenure cycle improved 806 -> 690 ms (target <650, not quite met) entirely via Round1; B mean 125,125 -> 129,770 (+3.7%, target >130.8k, not quite met, both win2 windows confirmed supply-bound via candidates->0/failed=0); view timeouts rose 25->47 (an open, unexplained safety regression). Prediction 84 overall: not confirmed. |
| S16 | Commander's ruling on S15b (6cj): hypothesis G is HALF right. Skipping the block gossip fallback (fired 7/7, the blockSubscriber share of bc.lock gone) cut Round1 124 -> 63 ms, the in-tenure cycle 806 -> 690 ms and the hand-over cycle 1252 -> 1001 ms; Round2 did not move (353 -> 362 ms, unimodal, p90/median 1.18). B mean 129.8k (+3.7%, at the edge of the noise floor, both second windows supply-bound), so prediction 84 is not met as registered and the standing best is unchanged. NOT adopted yet: view timeouts rose 25 -> 47 and that must be placed first. Round2 is now the largest single item of the cycle: a steady ~360 ms that scales with block size (8 ms on empty blocks) and is none of e.mu, JournalVote, BLS, the deferred check, or the gossiped block. Blind spot found in S14's method: Go's mutex/block profiles do not see goroutines blocked inside cgo, so a wait for the MDBX writer (mdbx_txn_begin) is invisible to them, and `pqc2cv` stamps the vote when the engine EMITS it, not when it is published. Suspect: the follower's serial processOutputs loop (service.go:585-704) publishes the commit vote only after earlier outputs finish, and CommitToCanonical/persistState in that loop wait for the single MDBX writer behind the block write (~204-349 ms, size-dependent). S16a: code trace + existing `canon`/persist timings in the kept logs; place the 47 view timeouts | logs + code trace (r35zzzb-keep, r35zzza-keep) | not written (diagnostic) | done (6ck) | S16a (6ck): H2 inconclusive. The output-loop mechanism is real and code-confirmed (processPrepareQC journals under e.mu then emits onto a 1024-deep channel drained strictly serially by processOutputs, service.go:585-596; OutputBlockCommitted is the one case handled fully inline -- CommitToCanonical+persistState, each its own MDBX write, service.go:642-716 -- while OutputSendToValidator/OutputBroadcast just spawn a goroutine and return), and the leader's own PrepareQC broadcast is structurally exposed to it the same way a follower's commit vote is. But CommitVoteSent/pqc2cv/PrepareQCFormed are all stamped BEFORE emit(), i.e. before the output even reaches the channel -- none of this campaign's stamps see the actual publish (PublishToTopic, service.go:876/889/896/922; SendRawBytes, service.go:975). The one correlation test these logs allow (a node's own commit-phases(n-1) vs its own r2kth(n)) shows no relationship in either round (Spearman rho -0.10 in 35zzza, +0.02 in 35zzzb) -- the mechanism does not explain r2kth's typical magnitude, though it isn't ruled out for occasional slow views. r2kth scales close to linearly with tx count in both rounds (8ms empty -> 327-333ms full) and correlates moderately (rho~0.35-0.39) with both leader and follower write time -- real but not tight. View timeouts: 35zzzb's rise (25->47 lines, 4->9 distinct views) includes one new flood-window full-block timeout (05:39:09) with no build stall and no fetch-by-hash/catch-up line in the 2s before it on any node -- not attributable to the switch via that mechanism, but genuinely unexplained. |
| S17 | Commander's ruling on S16a (6ck): H2 inconclusive and unlikely -- votes and broadcasts get their own goroutine once dispatched, and Round2 shows no relation to the same view's write (rho -0.10 / +0.02). The view-timeout rise was a line count, not events: 5 distinct events in 35zzza vs 6 in 35zzzb, none preceded by a fetch-by-hash, so N42_BLOCK_GOSSIP_FALLBACK=0 is adopted provisionally for diagnostic rounds. What is established about Round2: linear in the block's tx count (8 / 30-46 / 124-231 / 170-206 / 327-333 ms by bucket), unimodal, and it uses the same paths and message sizes as Round1, which is now flat at ~60 ms whatever the load -- so transport alone cannot tell the two rounds apart, and every stamp taken so far sits INSIDE the handlers. Nothing measures emit -> publish on the sender or wire -> handler entry on the receiver. S17: n42-r89 = r88 + those stamps only (sender: emit, dispatch, publish start/end, path rotor or gossip; receiver: earliest receive in the stream handler / subscription reader, then handler entry; per view, the k-th voter's id), same configuration as 35zzzb so the round also repeats its score | n42-r89 (diagnostic only) | to be registered by the agent: B mean within the noise floor of 129.8k; >= 80% of Round2 lands in ONE of: leader emit->publish, downlink wire, follower receive->handler, follower emit->publish, uplink wire, leader receive->handler | done (6cm) | S17 (6cm): prediction 85 partial. B mean 132,786 (+2.3% over 35zzzb, inside the 3.6% floor -- throughput repeats); in-tenure cycle 873 ms (+26.6% over 690 -- does NOT repeat within 10%). The six stamped segments (L1/D/F1/F2/U/L2) cover only 6.1% of Round2's 393 ms median -- every one is fast (single-digit to ~5 ms median), so clause (b)'s >=80%-in-one-segment bar is falsified decisively (largest segment U is 1.3% of Round2). Round1's three measurable segments do sum to 82.1% of its 70 ms (clause c met). Size law: none of the six segments scales with block tx count the way Round2 itself does (9ms empty -> 335ms full); the RESIDUAL (r2 minus the six segments) does, monotonically (1 -> 14 -> 132 -> 152 -> 326 ms), narrowing the culprit to the one unstamped interval in the whole chain: the leader's own PrepareQCFormed -> emit() gap (voting.go:208-231), which contains journalCommitVote's MDBX write against the same db handle the leader's own concurrent WriteBlockWithState uses -- the one-MDBX-writer contention hypothesis, now localized but not directly stamped. Commit votes succeed via the faster Rotor path only 13.5% of the time vs 100% for prepare votes -- a real asymmetry that does not explain Round2's magnitude either (rotor-path and gossip-path Round2 totals are statistically indistinguishable, both dwarfed by the unstamped gap). Zero build stalls, zero flood-window view timeouts this round (cleaner than both 35zzza and 35zzzb), arguing against attributing 35zzzb's timeout rise to the gossip-fallback switch specifically. Goroutine dumps (4 captured) show no goroutine parked in cgo/mdbx frames at capture time -- no positive evidence for the profiler blind spot, but the capture windows are (as before) inside the decay warmup, not the flood. |
| S18 | Commander's ruling on S17 (6cm): the six send/receive edges of Round2 sum to 24 ms of 393 -- transport, readers, output loop and handlers are all cleared, and both paths (Rotor 32 ms, gossip 21.5 ms) are equally fast. The whole residual (1 / 14 / 132 / 152 / 326 ms by size bucket) sits on the leader between `PrepareQCFormed` and the emit of the PrepareQC broadcast (voting.go, tryFormPrepareQC), and the only call in that gap that can block is `journalCommitVote` -> JournalVote's MDBX write transaction, taken under e.mu while the leader's own block write (349 ms, linear in block size) holds the node's single MDBX writer. S14 missed it because the wait is inside cgo (invisible to Go's mutex/block profiles) and the handler stamps did not cover QC formation. Score: 35zzzc repeats 35zzzb (132.8k vs 129.8k; first windows 140k = the eight generators' ceiling), so N42_BLOCK_GOSSIP_FALLBACK=0 is adopted for the bench and the standing best moves to 132.8k. S18: n42-r90 = r89 + a journal-duration stamp (jpvMs/jcvMs) + env switch that keeps the HotStuff safety journal in its own MDBX environment with the same durability flags (ordering and semantics unchanged: still durable before anything leaves the node); B1 runs with the switch off as the in-round baseline, B2 with it on | n42-r90 | to be registered by the agent: mechanism (B1 jcvMs ~ Round2 residual; B2 jcvMs < 10 ms, Round2 < 80 ms, in-tenure cycle down by >= 250 ms) separate from throughput (capped near 140k by supply; occupancy reported) and safety (no BAD BLOCK, journal recovery tests) | measurement half launched 2026-09-21 09:33 EDT as 35zzzd (n42-r90 sha256 193bd320478a = r89 + jpvMs/jcvMs/jcvAt stamps only, single configuration, prediction 86 in 6cn). The separate-environment switch was NOT built: the agent stopped on the stop condition -- the ConsensusState record (HotStuffState table) is written standalone by JournalVote AND atomically with canonical chain data by CommitToCanonicalWith's hook (service.go:689, blockchain.go:1376), so it cannot move without splitting an atomic write (persistence.go:68-75 names the equivocation risk). Commander's ruling: correct stop; the fix becomes S19, a scheduling change that touches no safety semantics | round 35zzzd measured directly (6cp): leader jcvMs median 340 ms vs Round2 median 396 ms (85.9%; 86.9% on the >=140k-tx bucket); 94.1% of full in-tenure views have jcvMs >= 200 ms with a mean unexplained gap of only 52 ms; 98.8% of the time the leader's own journalCommitVote wait ends within 10 ms of that SAME leader's own block-write ending -- the collision partner named directly, not by elimination. Prediction 86(a) confirmed on the full-block bucket and overall median; not met at the two middle size buckets (~36.5%), reported as-is. Follower jcvMs is 0 ms at the median throughout; the 3-view leader-fast/r2kth-still-large subset is too small to characterize. B mean 109,468 (-17.5% vs 35zzzc's 132,786) despite an unchanged config, because both win2 windows stayed full (49.8-50.0% occ, unlike 35zzzc's supply-limited 44-47%) -- every phase touching MDBX (finalize/write/proc/jcvMs) grows 30-90% from win1 to win2 in both legs, tracking a system-wide AnonPages memory-pressure ramp (14-20 GB -> 74-79 GB) that starts partway through every leg and resets at leg restart; the SAME ramp, same timing, is present in 35zzzc too, just masked there by its own supply limit. All 7 flood-window view-timeouts this round fall inside the memory-pressure window. No BAD BLOCK; 2 self-healed build stalls, both at the ramp's own start minute. |
| S19 | the leader starts its own block write only after it has journalled its commit vote for that block (PrepareQC formed), with a short timeout so a view without a quorum cannot hold the write: the journal then finds the MDBX writer idle, and the 349 ms write overlaps Round2's 24 ms round trip and the QC->push state-root work instead of sitting in front of the PrepareQC broadcast. Journal ordering, durability and the single ConsensusState record are untouched. Open questions for the agent to answer from the code before building: does anything on the next build's path wait for the parent's write (prefill `persistWait`), and does CommitToCanonical for v then queue behind the write on the critical path | n42-r91 (env switch, default off; A/B by leg) | to be registered by the agent | queued 2026-09-21 10:12 EDT as 35zzze (chain-35zzze.sh, n42-r91 sha256 df2cf25426b0, prediction 87 in 6co; warm-up/A1/B1 switch 0, B2/A2 switch 1), behind the running 35zzzd. Commander's early read of 35zzzd's leader rows (full blocks, n=199, mid-round): when jcvMs >= 200 ms (58% of views) r2kth - jcvMs averages 68 ms, so the journal wait is the bulk of Round2 there; but 22% of views have jcvMs ~ 1 ms and r2kth still 365 ms -- in those the PrepareQC left on time and the k-th commit vote came back ~360 ms later, which points at the FOLLOWERS' own journalCommitVote colliding with their import write. S19 addresses only the leader half; the analysis must report follower jcvMs | round 35zzze measured directly (6cq): pooling both windows per leg, Round2 falls 403 -> 115.5 ms median (-71.3%) from B1 (switch off) to B2 (on), because the leader's own jcvMs collapses 334 -> 0 ms median once the journal goes first while the writer is idle (82% lwWhy=journal, 18%=timeout, 0%=abandoned in B2; every timeout case is Round2's own vote-gathering running past the 150 ms budget, none coincide with a genuine HotStuff view-timeout/TC event within +-1 view). The in-tenure cycle does NOT grow to compensate (795.7 -> 755.1 ms pooled, actually a hair faster); prefill persistWait/insertParent stay at 0 in both legs, CommitToCanonical stays sub-ms at the median in both legs, and followers are statistically unchanged -- no saved time reappears anywhere this task instrumented. B2win1 is +10.2% faster in raw TPS than B1win1 (140.9k vs 127.9k) but only +3.1% on the derived (handover- and win2-free) ceiling (230.2k vs 223.3k tx/s), so most of the raw gap is win2/handover composition, not the switch. Prediction 87: confirmed on mechanism and on 'nothing moved'; partial on the two specific numeric thresholds (Round2<80ms median -- B2 pooled median is 115.5ms, though win1's own 96ms and both windows' p10 42-58ms clear it; cycle down >=250ms -- actual improvement is 40.6ms). Leg-order confound (B1 always precedes B2) and win1's shared ~140k supply ceiling are stated plainly as what the design can and cannot control for. |
| S20 | Commander's ruling on S18 (6cp): prediction 86(a) CONFIRMED by measurement -- leader jcvMs median 340 ms (94% of full-block views >= 200 ms), 98.8% of those waits end within 10 ms of the leader's own block write ending, follower jcvMs is 0, and Round2 = leader jcvMs + edges closes with a 17 ms median error. (My mid-round read that a fifth of views pointed at followers mixed in warm-up rows; withdrawn.) The score of 35zzzd, 109.5k, is NOT a regression of r90: both second windows ran FULL for the first time and showed what supply-limited second windows had been hiding in every round -- every MDBX-touching and CPU phase grows 30-90% from win1 to win2 while system AnonPages climbs from ~15-20 GB to ~75-79 GB within the leg, with 7 flood-window view timeouts inside that stretch. Suspect: the node heaps reach GOMEMLIMIT (9 GiB) late in the leg and the collector runs continuously (the 35q/35j signature). Also found: the harness's +150 s profile captures fall in the ramp, before win1 -- none of this campaign's profiles sampled a scored window. S20: profile INSIDE win1 and win2 (CPU + heap; commander's external capture is running on 35zzze at +250 s and +345 s), then decide between GOMEMLIMIT/GOGC and the allocation work queued as S6 | harness/diagnostic | not written | next (capture running) | round 35zzze measured (6cr): the requested t250/t345 pprof captures do NOT land inside win1/win2 -- independently re-derived window times (matching the round log's own block counts exactly) put win1/win2 4-5 minutes AFTER the captures, still inside each leg's 400s baseFee-decay phase; confirmed by all 8 CPU profiles reading a flat ~9% of one core with no shift, inconsistent with a 32-worker node mid-full-block production. The r35zzze-mem.log (which DOES span the true windows) is the authoritative comparison: per-node FILE-resident memory (MDBX mmap pages) roughly halves win1->win2 in BOTH legs (3295->1724 MB B1, 3611->1691 MB B2) while per-node Anon rises only ~4% over the same interval -- a squeeze, not simultaneous growth. Write-probe p90 heldMs/waitMs/commitMs all grow win1->win2 in both legs too (heldMs 156->228/222ms, waitMs 0/1->47/88ms). In every heap snapshot captured (even though mistimed), the dominant (51-73%) and, where growth is visible, the ONLY meaningfully growing allocator is qmdb.newMapIndexSized -- a plain Go map holding one entry per live key, per its own doc comment 'the largest single allocation in a loaded node', never partially evicted, and torn down only at process restart. Restart/on-disk dissociation confirmed directly: B2's win1 is as fast as (slightly faster than) B1's win1 despite strictly larger on-disk chaindata, ruling out disk size as the driver. Verdict: names a specific, well-evidenced cause (the QMDB live-key index's unbounded, restart-only-reset growth competing with page cache under a fixed 9GiB GOMEMLIMIT) without a direct live-key-count measurement to close the loop; the one addition named for the next round is a periodic mapIndex.Len() (or existing IndexStats() counters) log line at the mem-log's own 10s cadence, in the harness, not the node.  round 35zzzf's VM sampler (6cv) confirms directly, via OS fault/scan counters rather than Cached/AnonPages alone: pgmajfaultD/pgscanKswapdD/refaultFileD jump 17-200x between the pre-flood ramp and win1 (B2 pgscanKswapdD: 4,428 -> 885,875/10s, 200x) and stay elevated through win2, while per-node RssFile keeps roughly halving win1->win2 (both legs) even as the fault-RATE counters ease off their win1 peak (a saturation effect -- less file-backed memory left to evict, not less pressure). MDBX-touching phases (execMs +35.9% B2, leader write +45.8% B2, write-probe heldMs/waitMs both up sharply) grow somewhat more than pure-CPU steps (copy/push +9.5-28.6%) but not by a clean margin -- page-cache thrash is confirmed as present and dominant, with a smaller possible CPU/GC contribution not ruled out. Supply confirmed not the constraint again (candidates==included, both growing win1->win2). No heap profile captured this round (harness's own gasUsed/gasLimit>=0.95 trigger never fired since full blocks run ~50% occupancy here); 6cr's qmdb.mapIndex candidate remains untested by direct measurement. |
| S21 | Commander's ruling on S19 (6cq): the MECHANISM is confirmed -- with the write started after the journal, leader jcvMs 334 -> 0 ms and Round2 403 -> 115 ms, 82% of writes released by the journal (lwWait ~100-120 ms), no BAD BLOCK, every block written. But the in-tenure cycle did not move (715 -> 720 ms in win1), and the agent found the saved ~290 ms 'nowhere'. That cannot be: a cycle that stays put while one of its segments loses 290 ms means the segments were never a serial sum. Reading of the numbers: before, write(v) ran 0-349 ms, the CommitQC came at ~460 and the ~271 ms QC->push work followed -> ~730; now the CommitQC comes at ~180 but the write runs ~100-450 and the push of v+1 still lands at ~720 = write end + ~271. Hypothesis W: the leader's seal/state-root work for v+1 cannot start until its own block write of v has finished (a shared QMDB/MDBX resource), so the real serial chain on the leader is write(v) 349 -> root/seal(v+1) 271, and the CommitQC merely has to arrive before that chain ends. If W holds, S19's switch is neutral on its own (not adopted) and the pole is write+seal serialisation. S21 (logs only, r35zzze-keep): in B2, place write(v) start/end, CommitQC(v), and the start/end of each leader step between CommitQC(v) and push(v+1) on one timeline; does the first post-QC step start at the write's end? name what it waits on (file:line) | logs + code trace | not written (diagnostic) | next | hypothesis W confirmed in corrected form (6cs): a formula bug was found and fixed first -- every prior section's push_instant=tMs-write/1e6 silently computed writeStart, not push, once S19's lwWait sat between them, correcting B2win1's own cycle 720.3 -> 656.0ms (B1 unaffected, lwWait=0 throughout). Core test: push(v+1)-gate=max(write(v)_end, CommitQC(v)) is tight and IDENTICAL across legs (254.0ms B1, 254.2ms B2), while push(v+1)-CommitQC(v) is NOT (254.0 vs 467.0ms) -- write(v)_end is the later/gating event 0% of the time in B1 (Round1+Round2 always slower than the write) vs 92% of the time in B2 (Round2 collapsed below the now-delayed write). 6cq's saved 288ms was never on a path that fed the cycle once the write becomes binding -- 'nothing moved' was correct, this explains why. Code trace ruled OUT WaitBlockPersisted (bypassed by the near-universal speculative-build-hit path, parked==hit counts to within 0-1 on all 7 nodes, 0 discards/align-failures) and CheckSealParentApplied (0 stale-drops, non-blocking snapshot check); named the write/bc.lock/MDBX-single-writer serialization as the best-supported remaining candidate (REAL dependency, not incidental) without file:line certainty -- the one addition needed is tMs on the speculative-build-parked/hit and commitWork-begin lines, absent from this round's binary. 6cb's two-segment model corrected in place with a dated note: its first segment is WAIT for max(CommitQC(v-1), write(v-1)_end), not simply the consensus round-trip -- indistinguishable until S19 made Round2 fast enough to expose the write side. Derived consequence table: (i) Round2=115ms pooled + dependency removed = 434.5ms; (ii) write as today but no wait = 415ms; (iii) write 100ms faster alone = 495.5ms (still write-gated). |
| S22 | Commander's ruling on S21 (6cs): hypothesis W CONFIRMED in corrected form -- push(v+1) follows max(write(v) end, CommitQC(v)) by a leg-invariant ~254 ms (B1 254.0, B2 254.2), and with the agent's push-instant bug fixed S19's switch DID shorten the in-tenure cycle, 715 -> 656 ms (-8%): in B1 the CommitQC is the later gate (~461-497 ms), in B2 the write's end is (~342 ms, 92% of views). So N42_LEADER_WRITE_AFTER_JOURNAL=1 is adopted provisionally, and the leader's own block write is now ON the critical path (the old S5 candidate, measured at last). One attribution in 6cs is overruled: the ~135 ms inside the 254 ms constant is NOT paceBlock -- the kept logs show `miner: pacing wait` firing ~238 times a minute only during the baseFee-decay phase of empty blocks and ZERO times in the flood minutes (11:22-11:24, 11:36-11:39). Harness fact corrected again: the decay runs FIRST, so the scored windows sit at roughly leg start + 8..12 min; every profile this campaign captured (+150 s, and the commander's +250/+345 s) sampled empty blocks. S22: n42-r92 = r91 + ms stamps on every leader step between the gate and push(v+1) (speculative park/hit, commitWork begin, receipts copy, seal, BLS, push) and the wait for whatever lock ties push(v+1) to write(v)'s end; harness: captures keyed to the first full block of a leg, plus per-node majflt and smaps_rollup every 10 s for S20; both switches on in every leg | n42-r92 (diagnostic; config = 35zzzc + N42_LEADER_WRITE_AFTER_JOURNAL=1) | to be registered by the agent | launched 2026-09-21 12:58 EDT as 35zzzf (n42-r92 sha256 ba1a2105e458, prediction 88 in 6ct; both switches on in every leg; captures keyed to the first full block; 10 s VM sampler). U1 answered by reading (62439af7 handover): resultCh is unbuffered (worker.go:414) with ONE consumer, resultLoop (:535), which runs handleSealed -- and inside it WriteBlockWithState (:831) -- synchronously; Seal's delivery goroutine (adapter.go:903-905) blocks on that channel until resultLoop is free. So v+1's sealed result cannot be pushed until v's write returns: the single goroutine is the gate, no lock needed | round 35zzzf measured (6cv): U1 confirmed as a REAL dependency by code (6ct) but shown to NEVER bind this round -- resQWaitMs medians to 0ms in every window (all four), and resultRecv(v+1)-writeEnd(v) is positive and large (228-298.5ms, 0% within 10ms) because v+1's own build (650-850ms, buildBegin->specParked) unconditionally outlasts v's write (227-331ms); resultCh never has anything queued behind it. The ~254ms constant from 6cs decomposes to only ~131.5-133.5ms of named steps this round (100% placed, clearing prediction 88(b)'s 90% bar) -- 6cs's own figure was dominated by a ~135ms pacing wait that doesn't apply to full blocks (paceDurMs medians to 0, as predicted); of the ~130ms that remains, roughly two-thirds is trigger->specHit (this node's own worker goroutine finishing its still-running build, not a cross-block resource wait), the rest is receipts-copy (~32-36ms, real work) and push (~18ms). In-tenure cycle win1 680.0ms(B1)/691.0ms(B2), close to 35zzze's 656.0ms; jcvMs stays uncontended at the MEDIAN in every window (0ms) but its tail grows sharply into win2 (p90 10->281ms B1, 3->234ms B2) as the leg's own memory pressure sets in; lwWhy stable at ~75-79% journal/21-33% timeout/0% abandoned. import_breakdown.py-equivalent (mandatory, n=1926): body 11/proc 571/write 201/total 819ms medians. Safety: 0 BAD BLOCK/divergence/stalls; B1 2 timeout events (both decay/drain, none scored), B2 9 (7 decay, 1 flood at B2win1+9s). Derived: resQ=0 changes nothing (already 0); resQ=0 AND copy=0 -> 644.0ms(B1win1)/659.5ms(B2win1), a 4.6-5.0% ceiling on that specific pair of cuts. |
| S23 | take the leader's block write off resultLoop: handleSealed pushes/proposes v and hands the write to a dedicated, ordered writer goroutine (one block at a time, in seal order), so v+1's sealed result is handled as soon as it arrives. Everything that today relies on 'handleSealed returned => block written' must be found and kept true (WaitBlockPersisted, CommitToCanonical's need for the written block, the S19 latch, shutdown/flush, error handling of a failed write). Derived ceiling from 6cs: in-tenure cycle ~415-435 ms (sum of measured medians, not a prediction) | n42-r93 (env switch, default off; A/B by leg) | to be registered by the agent | REJECTED 2026-09-21 -- n42-r93 retired |  FIRST READ of 35zzzg (commander): B1 (async off) 139.2k / 90.3k, B2 (on) 136.5k / 100.6k -- first windows flat as expected. BUT leg A2, the second leg started with the async writer, never produced a block ('decay done: 0 empty blocks produced ... the chain is not producing') and the harness still printed ROUND DONE. Until the analyst says why, n42-r93 is not used again; the next round runs n42-r92  Analyzed (6cx): S23 confirmed NEUTRAL as predicted -- cycle 651.5->656.0ms win1 (+0.7%, noise), resQWaitMs/wqDepth/wqWaitMs all 0 in every window both legs, jcvMs stays at the 0ms median floor a third round running. A2's failure traced by direct fleet-wide log correlation, NOT a shutdown/Drain bug: 11-16s BEFORE any SIGTERM, node5 (leader of view 8785) logged a full 5/5-vote CommitQC forming for hash f47f65...13d8ac whose OWN write then failed with ErrStaleSeal ('sealed block is stale, applied head moved past its parent') -- a burst of sub-second view churn (views 8784/8785/8786 within ~7s, vs the usual 650-900ms cycle) at B2's own leg-teardown let two candidates get sealed for the same view, and the KEPT one collected full quorum before its proposer's own write lost the applied-head race. This is the documented, pre-existing propose-before-write exposure (push_order.go:36-40: 'followers may have imported a block the leader then abandons') -- not new in S23 -- triggered for the first time by this round's leg-teardown timing. All 7 nodes show the identical 'persisted committed QC names a block this node does not have' on restart; A2 inherits a committed-but-unwritable parent and can never produce past it. Attribution to the async switch: plausible (never seen in any earlier round's A2, grep-confirmed) but not proven (the race mechanism itself predates S23). WIN2 (100.6k vs 90.3k): sits inside leg-to-leg noise -- three rounds (35zzze 95.1/93.7k, 35zzzf 92.3/93.5k, 35zzzg 90.3/100.6k) show no consistent B1-vs-B2 direction. VM sampler (Job 3) reproduces 6cv's page-cache-thrash shape a second time in both legs at comparable magnitude, no switch-dependent difference. COMMANDER'S RULING (6cx): REJECTED. The switch is neutral by its own mechanism clause (resQWaitMs 0 -> 0, wqDepth 0, win1 cycle 651.5 -> 656.0 ms; B2's better second window is inside leg-to-leg noise: three rounds show no consistent direction), and the only round that ran it ended with a block carrying a full 5/5 CommitQC (view 8785, f47f65..13d8ac, would-be 13661139) that NO node stored: the proposer's own write was rejected as ErrStaleSeal after the QC had formed, and leg A2 could not produce. Prime suspect: r93's relaxed CheckSealParentApplied (accept the writer's ExpectedParent) let a seal through that the strict pre-check would have dropped BEFORE proposing -- under PROPOSE_BEFORE_WRITE a write that fails after the QC is not 'an ordinary sibling race'. S23b is establishing whether the hazard is reachable on the main line too; it goes to docs/OPEN_ISSUES.md either way |
| S24 | Commander's ruling on S22 (6cv): the seal-path stamps overturn the last model again, and this time they are direct. resQWaitMs is 0 in every window -- the single resultLoop goroutine (U1) is a real dependency that NEVER binds, because v+1's sealed result arrives 228-298 ms AFTER write(v) has ended. What the leader is doing all that time is building: buildBegin -> specParked is 650 / 611 ms (B1 / B2), and the in-tenure cycle (680 / 691 ms) = the leader's own build of the next block + receipts copy 32-36 + push 18. 6cs's 'max(write end, CommitQC) + 254 ms' was a correlation between things that all end before the build does; its 254 ms constant is 80 ms of waiting for the node's own still-running build plus copy and push. So S15b and S19 paid while the QC was the later event; now neither the QC nor the write is, and S23 (running as 35zzzg) is expected to be NEUTRAL -- prediction 89(a) will be falsified, which I record here before its result. The pole is execution: the leader's build ~611-650 ms (prefill ~142 with specTreeReload 112, parallel fill ~351, assemble/finalize/root the rest) and, right behind it, the follower's import 774-803 ms (exec 255-266, finalize 145-151, write 204-214). S24: anatomy of the 611-650 ms build on full blocks from existing lines (`miner: build phases`, `prefill phases`, `parallel fill`, seal path) -- which steps are serial by necessity, which overlap, and the three largest | logs only (r35zzzf-keep) | not written (diagnostic) | next | round 35zzzf's own logs re-examined at finer grain (6cw): leader buildBegin->specParked (650.0ms B1win1/611.5ms B2win1) breaks down as parallel-block execution sum 347.0/332.0ms (53.4/54.3%, internal/parallel_processor.go:560-640), assemble/state-root 150.4/144.5ms (23.1/23.6%, worker.go:1547 -> root computer, already flagged by 6cb's own title), and an unaccounted remainder of 91.8/74.2ms (14.1/12.1%, worker.go:1244-1420 + 1909-2117) that miner: prefill phases -- which never fired as an outlier on any of the 96-98 sampled full leader blocks this round -- rules OUT as its own listed steps' content. VERDICT on the sums-within-10% bar: inconclusive (12-22% short depending on leg/pick-inclusion), though every major bucket (parallel exec, state-root, MDBX write) is ranked and attributed. Follower side reconciles cleanly (hdr+body+proc+write = 673.1 vs total 679.8, 0.99% gap) since blockimport.proc wraps parallel_processor.go directly with no leader-style commit()/task-park wrapper outside it. The coordinator's own working hypothesis (leader fill slower than follower exec) does NOT hold: follower execMs is actually 7.9-9.4% SLOWER than leader's (222.5 vs 205.0ms B1win1), waves/aborts/fallback identical (1/0/false) both sides -- the real, measured asymmetry is parallel-block.finalizeMs (leader 17.5ms vs follower 126.5ms, a ~109ms gap running the OPPOSITE direction, not chased to root cause given time budget). Idle gap between builds on the same leader: 21.0/19.0ms median (n=6, small sample) -- essentially none. win1->win2: every step grows in both legs (leader TOTAL +22.5%/+39.0%, state-root +23.8%/+25.3%, follower proc +17.1%/+30.9%), consistent with 6cv's page-cache thrash, B2 growing harder throughout. Derived: (i) specTreeReload=0 -> unchanged (680.0/691.0ms, already ~0 for full blocks); (ii) leader fill matched to follower exec -> unchanged (leader already faster, no headroom on the measured direction); (iii) idle gap removed -> 659.0/672.0ms; (iv) all three -> same as (iii), 659.0/672.0ms. COMMANDER'S RULING (6cw): accepted as inconclusive on its own bar (12-22% unplaced) but decisive on priorities. Leader build 611-650 ms = parallel execution ~332-347 (53%) + assemble/state root ~145-150 (23%) + pick ~61 + ~74-92 unplaced (worker.go:1244-1420, 1909-2117); no idle gap between builds (19-21 ms); my framing that the leader's fill is slower than the follower's exec was wrong (execMs 205 vs 222.5, both 32 workers, 1 wave, 0 aborts). Follower import = exec 222 + write 195 + finalize 126. Every step on both sides is execution or MDBX work over the same ~160k transfers, and every one of them already runs under the page-cache thrash of 6cv from win1 on (major faults 20x the ramp's). So S25 (memory) outranks any single-step optimisation: it is the one lever that touches exec reads, state root, finalize and write on all seven nodes at once, and it decides how much of the 205-222 ms exec is page faults rather than EVM work. After S25: the ~80 ms unplaced build preamble (stamps), then per-transaction cost (old S6/S4) |
| S25 | S20 ruled (6cv): the within-leg slowdown IS page-cache thrash, measured with OS counters: from ramp to win1 major faults rise 1.2k -> 25.7k per 10 s, file refaults 429 -> 21.6k, kswapd scanning 54k -> 1.1M (17-200x), while per-node RssFile falls 4.2 -> 2.9 -> 1.45 GB and RssAnon climbs to ~10.3 GB (7 nodes ~72 GB + generators on a 137 GB box, against 7 x 40 GB of MDBX). MDBX-reading steps grow most (exec +36%, leader write +46%). The lever is anonymous memory per node; what fills it is NOT yet known (heap inuse was only 1.0-1.5 GB, but every heap profile so far was taken in the empty-block phase). 35zzzg's fixed captures take heap profiles inside the windows; choose the GOMEMLIMIT / pool-size experiment from those numbers, not before (35q showed heaps at 7 GiB collecting on every block) | config round after 35zzzg's in-window heap numbers | not written | launched 2026-09-21 15:46 EDT as 35zzzh (harness only, n42-r92, prediction 90 in 6cy): GOMEMLIMIT A/B by leg -- warm-up/A1/B1 10GiB (today's value; GOGC=200 everywhere, set only in the run script), B2/A2 6GiB. Capture detection tested offline (10/10 canned-JSON cases, dry run wrote 4 files); windows now keyed to bench-run.sh's 'all 8 flood(s) submitting' line + 15 s, RPC poll as fallback; samplers add CPU-seconds per node and per generator, generator RssAnon, and the heap?debug=1 MemStats trailer every 30 s | round 35zzzh measured (6da): live heap in the flood is 5.4-7.5GB (B1 5.78-7.46GB win1->win2, growing; B2 5.49-5.71GB, flat -- both well below their own 10GiB/6GiB limits on their own). Top heap subsystems (all captures agree within 1-2pp): txlookup.Tail.Add 18.0%, senderCachePut 16.2%, qmdb.newMapIndexSized 13.6%, RLP/tx-decode family cumulatively ~25-29% (RPC BatchRawTransaction decode alone is 28.9% cum). 6GiB leg confirmed as a GC-thrashing failure, not a supply/occupancy effect: TPS collapses 5.0-5.7x (128.5k/95.1k -> 21.7k/19.0k) at IDENTICAL 50% occupancy; fleet CPU-seconds per block rises 7.4x (98.8->732 CPU-s/block) while TOTAL fleet CPU stays flat across an 8x throughput drop -- the CPU didn't vanish, it stopped doing block work and started doing GC work; NumGC rate rises ~10-15x (B1 ramp ~10.5/min -> B2 flood ~115/min). Arithmetic: GOGC=200 wants live x3 (~16-22GB) regardless of leg, so GOMEMLIMIT is ALREADY the binding constraint at 10GiB, just far more severely at 6GiB where live heap alone (5.4-5.7GB) nearly equals the limit. No value in 6-7GiB is comfortable on today's numbers; live heap would need to roughly HALVE (to ~3-3.5GB) via shrinking (1) the pool/sender-cache/txlookup family (~44-45% combined) and (2) QMDB's own mapIndex (13.6%, 6cr's long-flagged candidate). Job 3 (generators): NO DATA -- the vm.log's gens: field is present in format but empty on every line, a sampler defect, not a finding. Job 4 (retroactive fleet-wide safety check, 10 rounds, 48,809 committed heights): confirms 35zzzg's height-13661138 conflict is the ONLY one found anywhere; zero conflicts in the other 9 rounds -- recorded as a dated paragraph in OPEN_ISSUES.md. |
| S26 | SAFETY, outranks throughput: in 35zzzg honest nodes COMMITTED two different blocks at height 13661138 in consecutive views (8784 `7a6d85..`, 8785 `f47f65..`, same parent, 5/5 votes in both rounds each; node5 raw log; OPEN_ISSUES.md). S23b classified the hazard as reachable on n42-r92: the leader proposed a stale speculative sibling and the only check that caught it ran at write time, after the CommitQC; the followers voted for a block that does not extend the block they had just committed (deferred-vote path; extends-check fails open). S26: (1) a regression test that replays exactly this and fails today; (2) the vote rule refuses a proposal that does not extend the voter's last committed / locked block, on the prepare AND the commit vote, deferred path included; (3) the leader never proposes a sealed block whose parent is not its current committed-or-own-pending tip (check before push/propose, not only at write); (4) one confirmation round on the standard shape: no score regression beyond the noise floor, zero conflicting commits (new harness check: per height, at most one committed hash across all nodes' logs), and the harness marks a round ABORTED when any leg refuses to measure | n42-r94 = r92 + fix (no switch: safety) | to be registered by the agent | CONFIRMED 2026-09-21 -- n42-r94 is the base binary (safety) | confirmed in a full fleet round (6dg): n42-r94 (r92 + fix e49ce151), B1/B2, GOMEMLIMIT=10GiB throughout -- height_conflict_check.py finds 0 conflicts across 3,355 committed heights, 0 'commit vote REFUSED' lines on any of 7 nodes (Round1's own silent refusal cannot be separately logged/counted). 365-378 sealed-block-is-stale/write-failed-after-proposal events per node are the SAME normal background rate as every prior round (speculative builds outracing real progress), none followed by a vote/QC (0 refusals confirms this); the fix's own new leader-side guard (suppressing divergent same-height sibling, pre-push) fired exactly once (node6) -- the one genuine collision this round, caught before either candidate was ever proposed. Cost is real: Round1 rose from ~60-72ms (35zzzf/r92 baseline) to 153-262ms, landing close to the ~180ms the fix's own design predicted; lwWhy shifted hard to timeout (67-96%, up from 17-33%); first-window TPS (130.0k/135.3k) sits below 35zzzf's 141.0k/137.5k and 35zzzh-B1's 128.5k but inside this campaign's own established round-to-round noise band on 3 data points, though consistently in the same (worse) direction on every metric checked. CommitQC(v) becomes the LATER event than the build (vote round back on the critical path) in only 33% of win1 views but 78-83% of win2 views -- a real, measured cost concentrated in the already-slow second window. import_breakdown's mandatory line (791ms) is unaffected, inside noise of 35zzzh's 813ms, as expected (the fix touches voting, not import). RECOMMENDATION: n42-r94 is the correct base regardless (safety is not optional), but its throughput cost should be tracked, not assumed zero, in comparisons against pre-S26 rounds going forward. COMMANDER'S RULING (6dg): safety clause confirmed -- 0 conflicting heights in 3,355 committed, all legs produced, one leader-side stale sibling dropped BEFORE push (the fix working), 0 commit-vote refusals. n42-r94 is the base from here on; safety is not traded for throughput. The cost is real and I record it rather than call it noise: Round1 60-72 -> 153-262 ms (the prepare vote now waits for the deferred per-transaction check, ~134 ms on a full block, more under win2's GC pressure), and the CommitQC is again the later event than the leader's build end in 33% of win1 views and ~80% of win2 views (0% in 35zzzf). First windows 130.0k / 135.3k sit below 35zzzf's 141.0k / 137.5k, inside the noise band but on the wrong side. The rule only needs the block's PARENT, which the header carries; it does not need the 160k-transaction check. S31 recovers the cost without touching the rule |
| S27 | Commander's ruling on S25 (6da): prediction 90 confirmed in the negative sense -- 6 GiB is catastrophic (21.7k / 19.0k TPS, 7.5-8.6 s per full block, ~115 GCs a minute) because the LIVE heap in the flood is 5.8-6.3 GB in win1 and 7.5 GB in win2, not the 1.0-1.5 GB every earlier profile showed (those were taken in the empty-block phase). With GOGC=200 the target would be live x 3 = 18-22 GB, so at GOMEMLIMIT 10 GiB the limit, not GOGC, already drives the collector, and as the live heap grows through a leg the headroom shrinks from ~4 GB to ~2.5 GB while the node allocates 10.4 GB PER BLOCK (65 KB per transfer). I withdraw my 'GC is not it' for the within-leg slowdown: that reading also came from empty-block profiles; S20's cause is memory on both fronts -- the collector squeezed against the limit AND the page cache squeezed by ~10 GB of anon per node. No GOMEMLIMIT value fixes this; allocation and live heap must fall. Live heap: txlookup.Tail.Add 1.04 GB (18%), senderCachePut 0.93 GB (16%), qmdb.newMapIndexSized 0.78 GB (14%), tx decode family 25%. Allocation per block: parallelApplyTx 9.9 GB/16 blocks flat, go-buffer-pool.Get 8.3, decodeEthereumTransaction 8.3, state.journal.push 7.3, rlp.decodeUint256 5.9. S27 (the old S6, now measured): line-level report of the top allocation sites from the in-window allocs profiles, then one change at a time, each proven offline first (bench/qs-replay with allocs per block) before any fleet round. Safety retro-check: 48,809 committed heights in 10 kept rounds, the only conflict is 35zzzg's | spec first (logs/profiles), then n42-r95+ | per change | closed: nothing shipped, see 6dc | spec written (6db): top 3 work-list items by derived MB/block-or-GB-live removed, lowest risk first -- (1) internal/parallel_processor.go:670-672 (parallelApplyTx): pass the per-block signer through instead of rebuilding it on the nil-fallback branch, which the profile shows absorbing 15.99GB cum of this function's own 30.36GB (~1.0GB/block); risk LOW, parameter wiring only. (2) modules/state/intra_block_state.go:520-552 (Reset, 6 call sites): clear(map) instead of make(map) for stateObjects/stateObjectsDirty/nilAccounts/logs/balanceInc, called ~160k times per full block (~3.2GB/block); risk MEDIUM, must bound retained bucket-array capacity across a small tx following a large one. (3) modules/state/journal.go:61,63: pre-size/reuse the entries slice and clear(dirties) instead of discard-and-remake (~7.3GB/block); risk MEDIUM, journal revert semantics must survive exactly. Two further items (decodeUint256 pointer->value, ~5.9GB/block, MEDIUM-HIGH risk/wider blast radius) round out a 5-item list; 2 candidates (NormalizeExecutionMessage, decodeEthereumTransaction's struct escape) were NOT ranked since no single-line fix could be named without guessing. CPU sanity (properly-sampled profiles, first time this campaign): combined GC-related CPU share 20.1% (win1) -> 53.8% (win2), a clean 2.7x confirmation of the win1->win2 growth mechanism. Sender-cache sizing (the task's own 16M-slot hypothesis) does NOT match reality -- it's already 1<<20 (1M) by product default, and an earlier round's own comment already measured shrinking it as not clearly worth it (0.7% of an 11.2GB heap); deferred to that existing analysis, not re-opened. COMMANDER'S RULING (6dc): closed with nothing shipped, correctly. Item 1 was a line-attribution artifact (branch never taken; benchstat p >= 0.056); items 2-4 re-proposed e414790f, reverted earlier for a measured 27.59% CPU regression. What the step DID buy: BenchmarkParallelBlockTransfers (291f2be7) as the standing offline yardstick, and the number that redirects the work -- the parallel executor allocates 6.25 KB and 48 objects per transfer, ~9% of the fleet's 68 KB per transfer. Nine tenths of the 10.36 GB per block that drive the collector to 54% of CPU in win2 are allocated OUTSIDE execution: ingest, gossip, pool, decode, libp2p buffers. S29 partitions them; S28 tests the other side of the same problem, the collector's headroom |
| S28 | Config-only A/B by leg, the OTHER direction from S25 (6da: 6GiB was catastrophic -- this asks whether MORE headroom helps). At GOMEMLIMIT=10GiB the limit, not GOGC, drives the collector (live heap 5.8-7.5GB, 10.4GB allocated per block, GOGC=200 would target ~3x live unconstrained); GC+allocation CPU is 20% in win1, 54% in win2 (6db's properly-sampled profile). warm-up/A1/B1 GOMEMLIMIT=10GiB, B2/A2=14GiB (GOGC=200 unchanged) -- doubles win2's headroom (~2.5 -> ~6.5GB), should roughly halve collections per block, at the cost of ~4GB more anonymous memory per node (~28GB fleet-wide) competing with the page cache 6cp/6cr/6cv already showed thrashing. Reuses run-r35zzzj.sh/chain-35zzzj.sh (retargeted from S27, which shipped no code to test) with both harness fixes: generator process-match pattern ([t]xflood -rpc -> [t]xflood.*-rpc, live-confirmed broken against 35zzzi's own r35zzzi-mem.log) and capture_win's per-leg filenames ($leg instead of bare $1, which resolved to the same string as $win). Gate check (reported, not changed): worst case 7x14GiB (98GB) + generators (~20GB estimate, never actually measured before this round's own sampler fix) leaves ~19GB, at/below the 20GB watchdog -- but 6da's own 10GiB measurement (live heap only 58-75% of ceiling) makes the realistic case (~90GB total, ~47GB free) comfortable; box is 136.6GB, current MemAvailable ~104GB, above the 100GB start gate | n42-r94 (NOT r95 -- S27 shipped nothing; BIN=n42-r94 one-line switch at chain-35zzzj.sh's own top, flip to n42-r92 if 35zzzi ends ABORTED/unsafe) | 93 (6dd): (a) NumGC/min in win2 falls >=35%, GC+alloc CPU share falls from ~54% to <=40%; (b) per-node RssAnon rises 2-4GB, fleet pgmajfault/workingset_refault_file in win2 RISE, magnitude reported; (c) win2 block time improves if (a) outweighs (b), else it does not -- either is the measured result, 1.6-1.8s is no effect, win1 stays within noise; (d) S26 checks stay green in every leg including whether 14GiB legs produce at all | ABORTED 2026-09-21 19:00 EDT by the memory watchdog in the 14GiB leg |  COMMANDER'S NOTE: round 35zzzj ran B1 (10GiB: 127.2k @1.250 s, 83.1k @1.935 s) and was aborted in B2's ramp (GOMEMLIMIT=14GiB): `ABORT B2: MemAvailable 18G`. The builder had flagged this worst case (7 x 14 GiB + generators leaves ~19 GB against the 20 GB watchdog) and I accepted the risk; it materialised. I killed chain-35zzzj.sh by PID (321812) because its retry loop re-runs a round that dies on the memory floor up to three times -- that would have repeated the same abort and held the box I had just promised to n42-rs. Fleet down and both claim files gone at 19:04; gov5 stands down for >= 100 minutes. First reading: a higher GOMEMLIMIT is not affordable on this box with today's generator footprint; the analysis (light, niced) says how much the generators hold -- measured for the first time this round  Analyzed (6di): confirmed, with arithmetic, that 14GiB does not fit this 137GB box. At abort, 7 nodes' own RssAnon summed to 98.8GB (13.7-14.7GB each, already at/past the nominal 15.03GB decimal ceiling for several); 8 generators held a STABLE ~3.6GB total (measured for the first time, unchanged from B1's own ~3.5-3.9GB) -- the generators are NOT the driver, the nodes are. Shmem ~9.5GB stable both legs. Solving 7xlimit + gens(3.6) + shmem(9.5) + baseline(~7.5) + watchdog(20) <= 137GB gives a theoretical ceiling of ~12.8GiB -- 14GiB was already past it. B1's own low-water MemAvailable this leg was 41GB, comfortably above the watchdog. MAJOR separate finding: a true 20-second-delta allocs capture (fixed this round) gives B1's real allocation rate as 1.878 GB/block (11.5 KB/tx), confirmed within 5.5% against an independent MemStats TotalAlloc-delta cross-check -- roughly 5.5x SMALLER than every prior round's own reported 10.36 GB/block figure, meaning those earlier captures (6da/6db/6de/6df) were very likely not true 20s deltas. On the corrected scale, EXEC_shared drops to 4.77 KB/tx, now BELOW the isolated benchmark's 6.25 KB/tx -- the '4.4x gap' 6df investigated has mostly evaporated at the corrected measurement scale (the H-bench mechanism itself -- memdb vs MDBX/QMDB, Finalize/block-end skipped -- remains real, just not the right magnitude explanation). Flagged as a correction 6dc-6df's own absolute numbers need; not re-derived in place given this task's light-work scope. Prediction 93(a)-(c): cannot be judged, the leg aborted before any window opened, stated as such. (d): 0 conflicts on B1's own logs. VERDICT: falsified (14GiB is not affordable at all, not merely inconclusive). COMMANDER'S RULING (6di): prediction 93 FALSIFIED in the plainest way -- the box cannot afford 14 GiB: at the abort the seven nodes held 98.8 GB (13.7-14.7 GB each, still climbing), generators only ~3.6 GB (NOT the driver; measured for the first time), Shmem ~9.5 GB, baseline ~7.5 GB. Ceiling by arithmetic ~12.8 GiB, with no margin; a normal 10GiB round already bottoms out at 41-45 GB MemAvailable. So the collector's headroom cannot be bought with memory; allocation and live heap have to fall. The TRUE allocation numbers (allocs?seconds=20, agreeing with TotalAlloc within 5.5%): 1.88 GB per full block, 11.5 KB per transfer per node = executor shared by build and import 4.77 + follower decode/import 2.93 + unassigned 1.57 + leader build 1.03 + RPC ingest 0.75 + pool 0.45 + gossip 0.04. HeapAlloc climbs 6.4 -> 8.8 GB across the two windows, ~38-40 collections a minute. Gossip and ingest are small; the executor (matches the benchmark's 6.25 KB) and the follower's re-decode of blocks it already holds in its pool are the two places that matter |
| S29 | find where the OTHER 91% of the 10.36 GB/block go (6dc: the isolated executor is only ~9.2%). Partition the fleet's own in-window alloc_space by entry path (RPC ingest, gossip receive/send, pool internal, leader build, follower import) using go tool pprof -traces post-processed, keeping paths mutually exclusive by first-match-wins over an ordered pattern list. | n42-r92 (profiles + code reading only, no build) | n/a -- exploratory partition, no numeric prediction registered | done | done (6de): 85.1-85.7% of alloc_space assigned to named, mutually-exclusive categories (A ingest 4.9-5.0%, B gossip-recv 2.7%, C gossip-send/libp2p-mixed 4.6-4.9%, D pool-internal 3.3-3.4%, E leader-build-attributable 5.6%, F follower-import-attributable 20.8-20.9%, EXEC_shared (build+import, UNATTRIBUTABLE by stack trace -- the executor's worker-pool goroutines lose their calling context) 43.0-43.5%, unassigned 14.3-14.9%). F alone is 3.7x the combined attributable A+B+C+D, consistent with 6dc's confirmed redundant block-import decode. Ranked 5 candidates outside the executor, top: skip decodeChunkedBlock's unconditional RLP re-decode for pool-resident txs (internal/sync/rpc_chunked_response.go:132, up to ~13.2 KB/tx, product change, needs a new decode+import benchmark, medium risk). broadcast=0's actual propagation-mode meaning and the go-buffer-pool receive/send split were NOT resolved this pass. |  COMMANDER'S RULING (6de): accepted as a partition (85% assigned), but it CONTRADICTS 6dc and I will not average the two: 6dc's isolated benchmark puts the executor at 6.25 KB per transfer (~9% of the fleet's 68 KB), while this partition finds 27.6 KB per transfer in executor internals that build and import share (43%), plus 13.2 KB on the follower-import path (21%) and 5.6% on the leader build -- i.e. ~70% of the allocation is execution-side, 4-6x the benchmark. Either the benchmark is not representative of the fleet's executor (in-memory state vs QMDB/MDBX readers, no deferred-execution check, no post-state snapshots, no receipts/logs retention, 20k vs 160k transactions per block) or the fleet executes a transaction more than once per node per block (speculative builds that are discarded or restarted, CheckDeferredBlock's per-transaction pass on followers, the leader importing what it built). That question decides where the work goes and is S30. Ingest + gossip + pool are only ~16% together -- so my '91% is outside execution' reading of 6dc is withdrawn until S30 reconciles the two |
| S30 | reconcile 6dc (executor 6.25 KB per transfer in the isolated benchmark) with 6de (27.6 + 13.2 KB per transfer on execution-side paths in the fleet windows): count how many times one transaction is EXECUTED or per-transaction-checked on one node per block in the fleet (leader: speculative build attempts per sealed block, restarts, discarded fills; follower: CheckDeferredBlock pass + import; any node: re-execution on branch switch) from the kept 35zzzh logs, and compare the benchmark's entry path and state backend with the fleet's | logs + code (r35zzzh-keep, profiles) | not written (diagnostic) | next | reconciled (6df): H-multi's literal form (re-executing a committed block) is NOT found -- parallel block fires exactly once per distinct committed height on both profiled nodes (15/16 firings = 15/16 distinct heights, count 1 each), zero speculative builds discarded, zero same-height siblings suppressed in the capture span; the 16-block denominator used throughout 6da-6de is confirmed correct, not inflated. A real but MILDER form of multi IS confirmed: CheckDeferredBlock (internal/deferred_includable.go:38, follower-only call site internal/sync/rpc_block_push.go:91) runs deferredTxPlan, a SECOND full per-transaction sender-recovery-and-grouping pass separate from the executor's own -- already correctly bucketed inside 6de's own F, not an unaccounted addition. H-bench is the DOMINANT explanation for the 4.4x gap (27.6 vs 6.25 KB/tx): BenchmarkParallelBlockTransfers (internal/parallel_processor_bench_test.go) runs against lib/kv/memdb (in-memory, not MDBX/QMDB) and explicitly skips Finalize/block-end by its own doc comment ('neither block end nor Finalize runs'), plus decodes its 20k transactions once outside the timed loop -- three confirmed, named, structural gaps, not measurement noise. VERDICT: H-bench primarily, H-multi confirmed in a minor already-counted form. Correction to 6dc: its 9.2% figure is accurate for what the benchmark measures but should be read as ~20% (leader) / ~15% (follower) of the fleet's REAL per-tx executor cost once Finalize/block-end and the real storage backend are accounted for; 6dc's own conclusion (drop items 1-4, keep the benchmark as a within-executor yardstick) stands unchanged. No bucket correction needed in 6de. Breakdown of 10.36 GB/block: (i) one necessary execution ~5.1GB/49%, (ii) repeated/discarded executions 0GB/0%, (iii) follower decode+deferred-check ~2.15GB/21%, (iv) ingest/gossip/pool ~1.61GB/16%, (v) unassigned ~1.48GB/14%. COMMANDER'S RULING (6df): the contradiction was an ARTEFACT of my harness spec, found by refusing to average two numbers: `/debug/pprof/allocs` was fetched without `?seconds=`, so the profile was cumulative since process start and '165.74 GB / 16 blocks = 10.36 GB per block, 65 KB per transfer' was never true. MemStats TotalAlloc gives the real rate: ~1.6 GB/s in win1 = ~1.9 GB per full block, ~12 KB per transfer per node (~2.3 GB per block in win2). The benchmark's 6.25 KB per transfer for the executor is therefore about half of a node's allocation, matching the partition's ~49% execution share; there is no 4.4x backend gap, no repeated execution (S30 verified: one execution per node per block, plus the follower's sender-recovery walk in CheckDeferredBlock). Still valid: the shares, the live heap (5.8-7.5 GB), NumGC, GC at 20% -> 54% of CPU. Sections 6da/6db/6dc/6de/6df carry a dated correction; 35zzzj's capture now uses allocs?seconds=20 |
| S31 | the prepare vote gates on the block's HEADER being known (parent hash available -> extendsJustify can be evaluated), not on the deferred per-transaction check: the body arrives ~30-65 ms after the proposal and `peekBlockHeader` already exists in internal/sync/validate_blocks.go. The commit vote keeps its own extendsJustify check and its existing deferred/import gate. Both S26 regression tests must stay green, plus a new one: a proposal whose header shows a non-extending parent is refused at the prepare stage even though the deferred check has not run | n42-r95 = r94 + this | to be registered by the agent: Round1 back to <= 90 ms on full in-tenure views; CommitQC later than build end in <= 5% of win1 views; zero conflicting heights | CONFIRMED 2026-09-21 -- n42-r95 is the base binary | confirmed (6dj): Round1 recovered to 62-64ms win1 (essentially AT r92/35zzzf's own ~63-65ms baseline, not r94's 153-157ms), 115-115.5ms win2 (well below r94's 212-262ms, still above r92's ~70-72ms). Header-gate votes (531-545/node) dominate over the residual fallback (105/node). QC-later-than-build-end share improved (21.7-27.3% win1, 59.3-65.4% win2) vs r94 (33.3%/78-83%) but not fully back to r92's 0%. B mean 117.9k, ABOVE r92's own 116.1k and well above r94's 109.0k -- throughput recovered and then some. Safety: 0 conflicts/4,648 heights, 0 BAD BLOCK, 2 sibling-suppressions (working as designed). ONE THING FIRST fully investigated: all 35 'import-gated vote REFUSED' lines are ONE incident (view 6557, all 7 nodes) -- block a990bc...815fde was ALREADY correctly proposed/voted/committed in view 6556 via the NEW header gate, then its own hash resurfaced one view later carrying a SELF-REFERENTIAL JustifyQC (justifyBlock==blockHash); extendsJustify correctly refused it (proposal.go:667, the S26 fallback/import-gated branch at proposal.go:271-272, NOT S31's header gate); the view then timed out and formed a TC (~5-6s cost, one view only); NOT present in 35zzzi/r94 (grep confirms 0 there). VERDICT: the refusal itself is correct, not a bug -- the fix worked as designed with a small one-time cost. The upstream question (why a990bc's hash resurfaced with a self-referential justify, and why only in r95) is NOT determined, named as an open follow-up, not guessed at. RECOMMENDATION: n42-r95 becomes the base binary; safety unchanged from r94, throughput recovered above r92's own baseline. Delta-allocs partition not re-run this pass (time budget, safety investigation prioritized). COMMANDER'S RULING (6dj): prediction 94 confirmed on every clause -- 0 conflicting heights in 4,648; Round1 62-64 ms in win1 (35zzzi 153-262); win1 cycle 655/688 ms; first windows 141.7k / 140.5k, above r92's own; B mean 117.9k (second windows still supply-and-GC bound). The safety fix's cost is recovered; n42-r95 is the base. The one incident (view 6557) I read myself from node1's log: the leader of 6557 sealed TWO same-height siblings of the just-committed block (miner: suppressing divergent same-height sibling; re-proposing first sealed block, number 13658910/11), the engine dropped its own fresh seal ('sealed block dropped -- phase left WaitingForProposal') and the Proposal that went out carried the committed block a990bc.. as its own justify. Every follower refused it (extendsJustify: a block cannot extend itself), the view timed out, a TC formed, ~6 s lost once. The refusal is the S26 rule doing its job; the defect is on the leader side of the same tenure hand-over (a stale parked task re-proposed after its parent committed) and is the leader-guard half of S26 not being airtight -- logged in OPEN_ISSUES.md for a later step; it costs one view, never safety |
| S32 | followers stop re-decoding what they already hold: internal/sync/rpc_chunked_response.go:132 RLP-decodes every transaction of a pushed block although ~99.4% are in the pool, decoded, with their sender cached. Hash each transaction's wire bytes, look it up in the pool, reuse the pool's object on a hit, decode on a miss. Removes up to ~2.9 KB per transfer of allocation on six of seven nodes AND a second live copy of every recent block's transactions (pool + block cache), which is part of the 6.4 -> 8.8 GB live-heap climb. The leader already builds blocks out of pool objects, so sharing them with an imported block is an existing pattern -- to be verified, not assumed | n42-r96 = r95 + this; proven OFFLINE first (decode benchmark: B/op, allocs/op, ns/op with 0% / 99% / 100% pool hits; equivalence tests) | prediction 95 (6dk): (a) follower body+decode allocation bucket falls by >= the benchmark's own measured fraction x 0.7; (b) end-of-win2 HeapAlloc lower in B2 than B1 by >= half the derived duplicate-live-object figure, NumGC/block lower in B2; (c) follower import not slower, reuse >= 99% of a block's transactions; (d) S26 checks stay green, no BAD BLOCK, tx roots verified every block | CLOSED 2026-09-22 -- untestable on this harness; switch stays off; n42-r96 (switch off = r95) is the base | 6dl: reuse stuck at 0% on every node in both switch-on legs (node0 n=133 full blocks, sum(reuse)=0, sum(dec)=21,679,000, exact; same on nodes 1-6) -- traced to the fleet's own tx-gossip publish side delivering ZERO messages fleet-wide (grep for the receive-canary log "tx gossip: receiving" is 0 across all 7 nodes, whole round), because N42_TXPOOL_NOLOCALS=1 (baseline since 35z3/35z4) makes every RPC-submitted tx local=false, so NewLocalTxsEvent never fires and broadcastTxs has nothing to publish; combined with -shard-senders routing each sender to exactly one node's own pool, a follower's pool structurally never holds another node's share. Every S32 code path re-checked against the live wiring (env parse, WithTxPool gating, PeekHeader wiring, hash preimage, eligibility list) is correct -- not a bug in the patch. Prediction 95: UNTESTED, not falsified ((a)/(b) untestable at 0% hits, (c) throughput half-observed but its own correctness bar unmet, (d) safety CONFIRMED: 0/4495 conflicting heights, no BAD BLOCK, 0 refusals). B1 win1's own 116.7k @ 1.364s anomaly (vs 35zzzk's 141.7k @ 1.132s) is classified cold-cache-by-elimination (view timeouts 0/0, memory pressure identical to B2 win1, no safety incident) -- both windows are switch-off anyway. Recommendation: adopt n42-r96 as fleet base for S31's header-vote fix (reconfirmed clean); treat the reuse feature as dormant (default off) until either the propagation gap is fixed or prediction 95 is re-scoped around shard-senders + hint-only-ingest as this fleet's real tx path. 2026-09-22. COMMANDER'S RULING (6dl): the premise was mine and it was wrong. 6ce's '99.4% of a block's transactions are already known to the follower' measured SENDER-CACHE hits fed by the hint-only ingest (-hint-peers, round 35zi), not pool residency; on this fleet tx gossip never fires (N42_TXPOOL_NOLOCALS=1 since 35z4 suppresses NewLocalTxsEvent, broadcast=0), so a follower's pool holds only the ~1/7 of transactions it ingested itself over RPC. reuse=0 on every node was the correct outcome, not a bug: the code is sound, the ceiling on this harness is ~14%, and enabling tx gossip to raise it would add 6x ingest work per node -- the opposite of the goal. Prediction 95 untested; no fleet round is owed. Safety clean (0/4,495). The real transaction path on this fleet, for every later step: RPC shard (1/7, decoded into the pool) + sender hints for the rest + the pushed block (decoded once per follower). B1 win1's 116.7k is a cold-cache artefact of running the first full-block leg on a fresh reseed |
| S33 | spec: what holds the live heap (5.8 -> 7.5-8.8 GB climb squeezed against GOMEMLIMIT 10GiB, 6da/6di/6dj) and what can shrink it, from existing in-window heap profiles (35zzzh/i/k/l) -- no build, no benchmark | n/a (profiles + code reading only) | own goal: name owners covering >=80% of inuse_space, rank reductions, config-only first, answer whether GOGC matters under a binding limit | spec done | 6dm: four owners cover 93-94% of inuse_space -- decoded transaction objects (pool+block-cache+decode-garbage, ~1.5-2.2 GB, ~30%, the dominant win1->win2 grower at +652.6 MB of the leg's own +1204 MB climb), the tx-hash tail index (`internal/txlookup.Tail`, ~1.05-1.13 GB, ~16%, a PRODUCT-DEFAULT const already documented in-code as costing ~1.1 GB at this fleet's own 163k-tx blocks -- not a new finding, re-confirmed), the sender cache (`common/transaction.senderCachePut`, ~0.91-1.15 GB, ~15%, a HARNESS flag `N42_SENDER_CACHE_SLOTS=16777216` whose own code comment already measured its end-to-end effect as unmeasurable noise at every size tried), and QMDB's in-RAM live-key map (`lib/qmdb.newMapIndexSized`, ~0.77-0.78 GB, ~11%, flat across the leg, a product default with an existing-but-unwired MDBX-backed alternative, `UseMDBXIndex`, wired only into the offline replay tool). Top 3 ranked reductions: (1) sender cache 16M -> 4M slots, ~0.7-0.9 GB saved, config-only, zero code risk, the code's own comment already argues for this; (2) tail-index keep-window (`txIndexKeepBlocks`/`txIndexSealMinTx` consts in `internal/txindexer/indexer.go`) shrunk, ~0.5-0.9 GB saved, needs a small code change (add an env override) plus an A/B round, trades against a documented restart-rebuild cost; (3) wire `UseMDBXIndex` into the live node path, ~0.7-0.8 GB saved, needs its own build and A/B round given unknown MDBX-cursor-vs-map-lookup CPU/latency cost. GOGC question answered from the runtime's documented rule (next-GC = min(L*(1+GOGC/100), GOMEMLIMIT)): no, GOGC=100 or 400 would not change anything here -- the live heap (5.8-8.8 GB) sits above the limit-crossover point for every GOGC value in 100-400 (crossover at L=10GiB/(1+GOGC/100), i.e. 2-5 GB depending on GOGC, all below the measured range), so the 10GiB limit already binds every collection regardless of which of the three is set; only shrinking L itself (the reductions above) changes anything |
| S35 | reduction (1) from S33/6dm, config-only, no build: shrink the sender cache | n42-r96 (reuse switch off in every leg; header-vote fix only) | prediction 97 (6dn): (a) follower live heap lower in B2 than B1 by 0.6-0.9 GB at the same window, sender-cache owner ~1.15 GB -> ~0.3 GB; (b) sender-cache hit rate unchanged within 1 point, follower recov unchanged within 26 ms noise; (c) NumGC/block and GC's own CPU share lower in B2 win2, win2 block time better if (a)+(c) hold; (d) 0 conflicting heights, no BAD BLOCK | CONFIRMED 2026-09-23 -- N42_SENDER_CACHE_SLOTS=4194304 adopted for the bench | 6do: CONFIRMED. Sender-cache owner (go tool pprof, senderCachePut) 1180.8 -> 314.8 MB (-866 MB, inside the predicted 0.6-0.9 GB band); total inuse_space 7585.3 -> 7018.6 MB (-566.7 MB, diluted by an unrelated +282 MB growth in the tx-hash tail index, the same sealer-backlog mechanism 6dm already named). Hit rate (hintHits/hintFills) 14.4% -> 19.7% (up, not down -- no cost); recoverMs median 24 -> 42 ms (inside the 26 ms noise band) but p90 47 -> 215 ms (a real tail widening). Follower blockimport phases are FLAT (920 vs 921 ms total, proc 691 vs 684 ms) -- the win2 block-time win (1.667 -> 1.304 s, a new low outside every prior 10GiB round's own 1.463-1.875 s range, occupancy 49.3%, 46 blocks, 0 timeouts) is real but traces to the LEADER's own write phase (719 -> 374 ms, -48%) via GC CPU share roughly halving (gcDrain cum% 28.9% -> 14.4%, mallocgc 13.0% -> 7.3%; NumGC/min at peak ~40 -> ~28-34), not to anything on the follower's own import path. Safety clean: 0/11,905 conflicting heights, no BAD BLOCK; 31 refusals all at view 11741 (6dj's already-classified pattern, outside both compared windows); 2 sealed-block-dropped events whole round (S34's own baseline). Recommendation: adopt N42_SENDER_CACHE_SLOTS=4194304 for the bench (yes); try 1M (the product default) in one more round to see whether the recover-tail widening at 4M gets worse at a quarter the size. 2026-09-23. COMMANDER'S RULING (6do): prediction 97 confirmed -- the sender-cache owner fell 1.18 -> 0.31 GB, GC per minute and the collector's CPU share roughly halved in win2 (gcDrain 28.9% -> 14.4%), hit rate rose 14.4% -> 19.7%, and B2's second window ran at 1.304 s per full block (121.6k), outside every earlier 10GiB round's 1.46-1.88 s. Follower import is flat; the gain shows on the leader's write phase (719 -> 374 ms) and in fewer collections -- consistent with 6cv/6dm: win2 was the collector, not the import. 4M is adopted. I decline the 1M follow-up: the most it can return is ~0.24 GB more, and the recover p90 already widened 47 -> 215 ms at 4M -- a tail cost I would rather not deepen for so little. Next memory targets per 6dm: decoded tx objects (2.16 GB; pool 600k/200k harness cap vs 360k in flight -> config A/B on the pool cap and block cache 4 -> 2) and the tail index (1.13 GB, needs a code knob). Safety clean 0/11,905; 2 sealed-block-dropped events = S34's baseline |
| S34 | HotStuff-2 liveness: round 35zzzk view 6557 -- the miner's sibling-suppression path re-proposed this node's own already-committed block as a fresh candidate (importedParents fails open for a leader's own sealed block, since it is populated only by internal/sync's import-notification call sites); the resulting Proposal's JustifyQC equalled its own BlockHash, every voter correctly refused it (extendsJustify), and the genuinely correct next block was ALSO silently dropped by the unrelated phase-left-WaitingForProposal guard because the stale proposal had already advanced the phase -- one view (~5.3s) wasted for nothing | n42-r97 = n42-r96 + fix 7288c8f0 (three guards, no switch) | prediction 96 (6dp): (a) zero refusals/drops caused by a self-referential or already-committed proposal (35zzzn baseline, without the fix: 2 sealed-block-dropped events whole round, per S35's own result row); (b) 0 view-timeout events in the flood windows; (c) 0 conflicting heights; (d) first windows within noise of ~140k | CONFIRMED 2026-09-23 -- n42-r97 is the base binary | 6dt: CONFIRMED. Guard fired 0 times (incident did not recur); 0 REFUSED; 1 sealed-block-dropped/WaitingForProposal (35zzzn baseline 2), classified as a shutdown artifact (20:32:56, 6s into the memory-abort, 0 sibling-suppressions precede it). 0/9,089 conflicting heights, no BAD BLOCK, 0 view timeouts. B mean 124.3k (B1 128.7k/121.7k, B2 129.9k/116.8k) -- both win2 above 35zzzk's 16M baseline (95.0k/94.5k), confirming S35's cache win carries through. ROUND ABORTED 20:33:13 on an external memory spike AFTER both B legs' windows completed -- traced (journalctl) to a foreign Rust/C++ coverage build's cc1plus, not the qs fleet. n42-r97 adopted as fleet base. 2026-09-23. COMMANDER'S RULING (6dt): prediction 96 confirmed on all clauses (guard present and silent, 0 refusals, 0 flood-window timeouts, 0/9,089 conflicts); B mean 124.3k = the standing score on the safe lineage. The ROUND ABORTED tag is a foreign build: at 20:32:34 a coverage build under /home/n42/src/n42/n42-26 (tikv-jemalloc-sys, cc1plus) took ~47 GB in 15 s during our claimed teardown and tripped the watchdog; both B legs were already scored. .box-claim-rust appeared 20:34:22 |
| S36 | config-only, after S34's round: decoded-tx-object owner (2.16 GB, +653 MB across a leg): A/B by leg of the pool cap 600k/200k -> 400k/100k (the flood keeps 8 x 45,000 = 360k in flight; generators must not starve: report pending depth and any `txpool full`/rejected submissions) and, in a separate round, N42_BLOCK_CACHE_BLOCKS 4 -> 2 (report fetch-by-hash misses) | n42-r97, two rounds | to be registered by the agent: live heap -0.4..-0.8 GB per step, GC/min down, no starvation, no new fetches | S36a FALSIFIED (6dw), pool caps stay 600k/200k; S36b CONFIRMED 2026-09-24 (6dx): N42_BLOCK_CACHE_BLOCKS=2 adopted (win2 heap -310 MB, 0 fetch misses, TPS within noise) | 35zzzp queued; prediction 99 in 6dq; S36b = 35zzzq prepared  -- 6dw: FALSIFIED. 400k/100k starves the generators: occupancy 39.4% -> 13.0% in win2, TPS 121.7k -> 89.9k despite a faster blockTime (emptier blocks, not a faster chain); underpriced-rejection rate roughly doubled (907 -> 1826, node0); 0/11,258 conflicting heights. Do NOT adopt 400k/100k; 600k/200k stays the default. |

| S38 | spec: the tenure hand-over cycle (a new leader cannot speculate on a block it only just followed) -- measure hand-over share/cycle on 35zzzn, trace the code guard that blocks cross-view speculation, rank design options; no build, no round | n/a (logs + code reading only) | own goal: sums within 15%, name the guard file:line, rank options by ms-saved/risk | spec done | 6dr: hand-over share 53.9% (win1)/26.8% (win2); cycle 966/2046 ms (win1/win2) vs in-tenure 676/842 ms; timeline sums to 98.5% of the measured cycle (632 ms is the synchronous build alone, specParked 0/77 -- the speculative hint never fires, confirming 6cd in data). Derived +37,000 TPS (+29.8%) pooled if hand-over ran at the in-tenure rate. BLOCKER: `proposal.go:650`'s `sendVote` hint is gated on `importedBlocks[blockHash]`, false under the deferred-vote path for any large block. Ranked: C (prefill/pick overlap, ~60-80 ms, no protocol decision) > B (build on v's post-import, pre-write state; ~46-200 ms + unknown share of an unstamped ~366 ms gap; needs a protocol decision on building atop unwritten foreign state, gated by S26/S34's extends-checks) > A (as stated, not implementable -- CheckDeferredBlock never executes v). One missing stamp: InsertChain's own start, to split the 366 ms gap into queueing vs decode/pre-exec COMMANDER'S RULING (6dr): the biggest lever measured so far -- hand-overs are 54% of full blocks in win1, 966 ms vs 676; equalising them is worth a derived +37k (+30%). The blocker is confirmed (the speculative hint at proposal.go:650 never fires on the deferred-vote path: 0/77 hand-overs had a parked build), but firing it earlier buys nothing by itself: today the build already starts the instant v-1's import ends. What the timeline exposes is 366 ms of the hand-over leader's 566 ms import span that no line covers (decode+exec+finalize+write are ~200). That unknown is larger than Option B's 46-200 ms and Option C's 60-80, so S39 stamps it first (one diagnostic build, riding the S36b config round). Option B (build on a foreign block after exec+finalize, before its write) is APPROVED IN PRINCIPLE for after S39: it extends the existing own-unwritten-parent pattern to a block the leader has voted for, under the S26/S34 guards, discarded like today's speculative builds if v-1 is not committed; Option C is folded into it |
| S39 | stamp the block-push receive path (6dr's own 366 ms unattributed gap on a hand-over leader's arrived -> import_end): rxEndTMs/decStartTMs/decEndTMs/chkStartTMs/chkEndTMs/qTMs/insStartTMs (the MISSING-STAMP) under N42_CONTENTION_DIAG=1, appended to the existing blockimport phases line, riding S36b's own config round | n42-r98 = n42-r97 + stamps (diag(sync) d3069b37) | prediction 100 (6ds): (a) named steps sum >= 95% of arrived->import_end, report the largest item; (b) S36b win2 live heap lower in B2 by 0.1-0.3 GB, no new fetch-by-hash misses; (c) win1/win2 within noise of 35zzzp; (d) 0 conflicting heights, no BAD BLOCK | CONFIRMED 2026-09-24 (6dx) | 6dx: CONFIRMED. Named steps (pooled, n=2826): decode 42ms, deferredCheck 228ms (largest, WORK not a wait), queue-wait-before-InsertChain 23ms (p90 363ms, a real tail), InsertChain-internal total 792ms; sum=1086ms vs rxEnd->tMs wall 1113ms = 97.6%, over the 95% bar. Resolves 6dr's 366ms gap: deferredCheck dominates. Block cache 4->2: win2 heap -310MB (top of the 0.1-0.3GB band), 0 fetch-by-hash misses. 0/11,536 conflicting heights, no BAD BLOCK. 2026-09-23. COMMANDER'S RULING: the 366 ms of 6dr is resolved -- on every follower a pushed full block runs decode 42 -> deferredCheck 228 -> queue 23 (p90 363) -> InsertChain 792 ms, serially; the deferred includability check (per-tx sender/nonce/balance against the parent state) is 228 ms of WORK in front of every import and of every hand-over build. S42 runs it concurrently with the import (the executor validates the same facts; the check only has to precede the vote). The queue tail (p90 363 ms) is a second item for later |
| S40 | Option B (approved in principle, 6dr): the node that will lead view v starts a speculative build of v on v-1 as soon as v-1's execution+finalize produce its post-state, before v-1's write -- extending the own-unwritten-block speculative path to a foreign parent this node voted for | not built -- stopped | own goal: mechanism (which event triggers, file:line) + either a working build or a STOP with what is needed | STOPPED 2026-09-23 (6du) | trigger identified and fully explained (EventBlockImported, fired early by NotifyBlockExecuted, already reaches onBlockImported but the existing hint only fires from inside sendVote, which the deferred vote already raced past on every one of 6dr's 77 hand-overs -- a one-line fix). Post-state retention for VALUE READS is a small, additive fix (CapturePostState is source-agnostic; the existing executedHints hint mechanism already solves the ExecutedResult half). QMDB ROOT computation for an unwritten FOREIGN parent has no viable path today: NewMinerRootComputer's only unwritten-parent route (HasUnwrittenBuild/AdoptOwnAppends, internal/blockchain.go:601) is keyed to THIS node's own prior append sequence on its OWN isolated tree instance, never populated for a block this node only imported; the fallback (ReloadForBuild) requires the parent to already be the applied/written head. Both fail closed (no safety risk, just no speedup) -- but that makes the switch a permanent no-op on the QS fleet's own QMDB configuration. Fixing it needs either the isolated tree to replay a foreign candidate's own appends (new undo/peel bookkeeping, cross-goroutine since import and the miner build run on different goroutines) or deferring root computation out of the build's own fill/exec path -- both architectural, not additive; not attempted per the task's own no-redesign instruction COMMANDER'S RULING (6du): correct stop. Two facts: (1) the hint never fires on hand-overs because tryDeferredVote marks the view voted before execution, so onBlockImported's sendVote branch is skipped -- a one-line fix, but inert alone; (2) the miner cannot build on a foreign unwritten parent because the isolated QMDB speculative tree (bc.minerRC) holds only this node's own appends; a foreign v-1 would need its mutation sequence replayed onto it with undo bookkeeping, or root computation moved out of the fill path -- both architectural. Not attempted now. The cheap substitute is fewer hand-overs: S41 = leader tenure 4 vs 8 by leg (config only) |
| S41 | config-only, n42-r98: N42_HOTSTUFF_LEADER_TENURE 4 (warm-up/A1/B1) vs 8 (B2/A2), 10th run_leg argument; block cache 4, pool caps 600k/200k, sender cache 4M everywhere; round 35zzzs queued 2026-09-23 21:05 EDT behind 35zzzq + 10 free minutes | n42-r98 | prediction 102 (6dv) | PARTIAL 2026-09-24 (6dz) -- tenure 8 ADOPTED for the mechanism (chained cycle 919 -> 805 ms, win1 +5.9% while occupancy fell to 34%); the TPS gain is certified only once supply is raised | 6dz: PARTIAL. Hand-over share B1(tenure4) win1 20.0%/whole-leg 17.3% (below nominal 25%, below 6dr's 53.9%/26.8% on a different round); B2(tenure8) 0/37 sampled full blocks but likely selection-bias (occupancy collapse leaves too few full-block samples). Chained-only cycle 919 -> 805ms (-12.4%), a real, uncofounded speedup. Win1 TPS 122.9k -> 130.1k (+5.9%) WHILE occupancy falls 49.2% -> 34.1% (win2 14.2%) -- generator supply, not consensus, is the ceiling; TPS number cannot be certified as the tenure-8 result. Cost clean: 12 timeouts both legs, leader RssAnon within +0.5GB. 0/12,176 conflicting heights. Recommend adopt tenure 8 for the mechanism; re-judge TPS after S43 raises supply. 2026-09-24.|
| S42 | run CheckDeferredBlock concurrently with InsertChain (switch N42_DEFERRED_CHECK_CONCURRENT, default off): today decode -> check 228 ms -> queue -> import is serial on every follower and every hand-over leader (6dx); the import validates the same facts, the check only has to precede the vote | n42-r100 = r98 + this; round 35zzzt A/B by leg (tenure 4, block cache 2) | prediction 103 (6dy): (a) rxEnd->InsertChain start ~293ms -> <=80ms on full blocks; (b) hand-over cycle falls >=150ms (966 -> <=816), in-tenure cycle and hand-over share unchanged; (c) B2 win1 TPS above B1 by more than the 3.6% floor, or lower occupancy at the supply ceiling; (d) 0 conflicts, 0 votes for a block that failed its check, 0 BAD BLOCK | PARTIAL 2026-09-25 (6e4): rxEnd->InsertChain 286 -> 75 ms as predicted; hand-over cycle and TPS unchanged (supply-bound); safety clean -- N42_DEFERRED_CHECK_CONCURRENT=1 ADOPTED (latency win, not a throughput fix) | 6e4: PARTIAL. (a) CONFIRMED exactly -- rxEnd->InsertChain start 286 -> 75ms (n=954/870), matches the predicted 293->80ms almost precisely; rxEnd->tMs wall also falls 1058 -> 927ms. (b) NOT CONFIRMED -- hand-over cycle 2036 -> 2018ms (-0.9%), far short of the predicted >=150ms cut (different scale than 6dr's own 966/816ms reference round). (c) NOT CONFIRMED -- TPS +0.4% (inside the 3.6% floor), occupancy unchanged. (d) CONFIRMED -- 0/11,447 conflicting heights, no BAD BLOCK, 0 votes on the round's one failed deferred check. Also S44 (same section): the decisive number is the generators' own top-up rate (~1,200-1,320 tx/s aggregate) against the chain's ~115-126k tx/s consumption, a ~90x gap -- answer (iii), generators short of the sustained rate the chain needs, consistent with 6e3's own pool-promotion finding. Adopt the concurrency switch for its clean latency win; it is not a throughput fix. 2026-09-25.|
| S43 | config-only, n42-r100 (S42 switch off): supply by leg -- 8 x 1000 senders, target-depth 45000 (warm-up/A1/B1) vs 16 x 500, target-depth 22500 (B2/A2); same 8000 funded senders and 360k aggregate in flight (S7's shape, lost in 35zzy to the 52 s stall); 12th run_leg argument; round 35zzzu queued 2026-09-24 03:18 EDT behind 35zzzt + 10 free minutes | n42-r100 | prediction 104 (6e0) | FALSIFIED 2026-09-25 (6e2): sixteen generators of 500 deliver less (3rd time); generators are DEPTH-bound (0.9 -> 0.3 cores each, underpriced 763 -> 0, pendingAccts 62 -> 22), not CPU- or RPC-bound. Engine ceiling at tenure 8 (B1 chained full blocks): 839 ms per 163k = 194k tx/s. Declined: 16 x 45000 (= S1). Open question S44: why are blocks two thirds full while the pool holds 360k | 6e2: FALSIFIED (contended round, B1-vs-B2 comparison stands): B1(8x1000) 109.6k/106.3k occ 31.5%/21.4% vs B2(16x500) 76.3k/67.6k occ 11.7%/11.0% -- sixteen generators deliver LESS (repeats S1/S7). DEPTH-BOUND, not CPU/RPC: per-generator CPU 0.9 -> 0.3 cores despite double the process count; underpriced 763 -> 0; pendingAccts 62 -> 22 -- target-depth 22500 + depth-by-nonce throttles each B2 generator well below available headroom. ENGINE (B1 only, B2 has no back-to-back full blocks): 839ms chained cycle @ 163000 tx = 194,255 tx/s. Recommend: no further round on 16x500 as configured; retry 16 generators at target-depth 45000 (unhalved) if supply is still suspected. 2026-09-24.|
| S44 | 6e3: sampler added to 35zzzt's runner -- pool node0/node3 pending/queued (metrics port, txpool_pending/txpool_queued) every 10s in r35zzzt-vm.log, and per-leg generator .out/.err preserved to wr-logs/r35zzzt-floods/<leg>/ | n/a (harness only) | n/a | corrected | 6e4 (corrected): topup= is RPC BATCHES not tx (rpcbatch=200), pool= is the generator's own submitted-minus-mined ESTIMATE, not a pool reading. Aggregate submitted ~43,500-45,000 tx/s vs chain consumption ~115-126k tx/s (~2.6-2.9x gap, not 90x). topup=0 in 17-27% of samples when the estimate reads 3-6x above the 45,000 target -- primarily (iii-a) self-throttling on an inflated estimate, compounded by a real (iii-b) shortfall (active-second rate ~52-57k tx/s also short of consumption). txpool_pending is LIVE (changes on nearly every 10s sample), not reorg-gated. 2026-09-25.|
| S45 | config-only, n42-r100, S42 switch on: generator -target-depth 45000 (warm-up/A1/B1) vs 225000 (B2/A2), 12th run_leg argument. Why: txflood's own depth estimate (submitted - mined/share - failed) reads 155k-257k against a 45k target, so the generators throttle to topup=0 most of the time; 6e4's '1,300 tx/s' was a unit error (topup is RPC batches of 200 = ~33k tx/s per generator). Round 35zzzw queued 2026-09-25 05:20 EDT behind 35zzzt + 10 free minutes | n42-r100 | 105 (registering) | FALSIFIED as configured 2026-09-25 (6e6): 225000 drains the 4.5M budget in ~100 s; the flood-file capture is stale (identical files across rounds) -- being fixed. Commander's reading of node0's txpool_pending (vm.log): during B1's flood it swings 0 -> 110k -> 400k -> 570k -> 28k -> 0 in five minutes and reads 0 for many samples while queued stays < 7k: the pool is EMPTY much of the time -- the generators burst and starve on their lagging depth estimate, which is why blocks average two thirds full. Lever = steady pacing (S46) | 6e6: FALSIFIED at 225000 -- generators run dry mid-leg (win2 collapses to 715 TPS/0.1% occupancy). wr-logs/*-floods flood-log files proven STALE (byte-identical MD5 across rounds) -- generator-side (1)/(3) questions unanswerable from them, flagged as a harness bug. Fleet-side (vm.log node0 pool pending): both legs peak at the SAME ~795-799k ceiling (the pool's own 800k cap, not the target-depth); B2's own pending sits at 0 for the final ~1m42s vs B1's single last-sample zero -- B2 ran dry well before leg end. Budget arithmetic: 1000 senders x 4500 pertx = 4.5M tx/generator; at the one (stale, order-of-magnitude) offered rate on record (~44,460 tx/s), a flat-out, unthrottled generator (as 225000 effectively becomes, since real consumption outpaces any replenishment) exhausts its budget in ~101s, far short of the 13-min leg -- consistent with B2's own win1 decline (20.3%) preceding win2's collapse. 0/11,868 conflicting heights, no BAD BLOCK. Recommend depth 90000 + pertx 9000 (2x target for throttling headroom, 2x budget for survival margin) as the next configuration; fix the -floods capture before the next generator-focused round. 2026-09-25.|
| S46 | harness only: generator PACING by leg -- today's depth throttle (rate 0 / depth 45000 / pertx 4500) vs steady rate (16,000 tx/s per generator = 128k/s aggregate ~ consumption at tenure 4 / pertx 9000 so the budget outlasts the flood); 12th run_leg argument (triple, supersedes S45's own single depth argument at the same position); also fixes the flood-file capture | n42-r100 (unchanged), round 35zzzx | prediction 106 (6e7): (a) B2 pool pending never 0 during flood windows, stays 50k-500k, occupancy >=45%; (b) B2 TPS above B1 by >3.6% (win1>=135k, win2>=130k); (c) generators not dry (last-minute topup>0), discards <=2x B1; (d) 0 conflicts, no BAD BLOCK | ran 2026-09-25 07:35-08:39 EDT as 35zzzx, ROUND DONE then `ABORT A2: node 1 logged a BAD BLOCK` (first BAD BLOCK of the campaign -- under investigation before anything else). Raw: B1 (depth throttle) 127.3k/103.8k; B2 (rate 16000, depth 0, pertx 9000) 137.5k @1.111 s occ 46.9% / 100.5k @1.622 s occ 50% -- win1 +8%, but in win2 the pool overflowed (pending 600,002 + queued 200,000) once blocks slowed to 1.6 s |  COMMANDER: the BAD BLOCK is `open read transaction: db closed` at 08:39:38 during A2's teardown -- an import in flight when the node closed its DB, a shutdown artefact (OPEN_ISSUES line; the harness check must ignore lines after SIGTERM). Steady pacing is the first supply lever that moved the score; the overflow in win2 is the next thing to fix (S47: rate + depth cap combined in txflood) |
| S47 | txflood: let -rate and -target-depth combine (min of the two shorts); round 35zzzy by leg: rate 16000/depth 0 (35zzzx's B2) vs rate 16000/depth 300000, pertx 9000 | n42-r100 + new txflood binary | 107 (builder registers) | dispatched 2026-09-25 08:55 EDT, launch included | |

| S12b | Commander's follow-up: does not accept "follower import is off the in-tenure critical path" on the strength of one coincidence alone (cycle 799 ms == import total 803 ms). Hypothesis H: deferred execution's Round-2 gate needs the PARENT imported, imports are a serial per-follower chain, so a saturated depth-1 pipeline at ~800 ms/stage produces one QC per ~800 ms and import -100 ms => cycle -100 ms. Test H directly: per-(node,v) d1 = held-vote-release time minus the parent's blockimport-phases end; per-follower import-chain busy fraction and idle gap; same two measures for hand-over | logs only (wr-logs/r35zzz-keep) | done (6cc) | falsified. Import chain is not saturated (39-61% busy per window, bimodal idle gaps, not ~100%/~0). 73.4% of in-tenure full blocks need zero followers to hold a commit vote on import at all; the quorum-forming follower (4th-fastest of 6) has a held-vote line in only 13.9% of blocks, and even then never releases within 20 ms of its parent's import end (median 149 ms) -- the mechanism is real code (proposal.go:518-547, confirmed by reading) but exercised on a minority of blocks, not the pacemaker. 6cb's SLACK answer stands (partial, small), now for a mechanistic reason. Hand-over differs: the new leader's build never starts before its own import of the parent ends (100% of 68 rows, median gap 508 ms after import), so import is somewhere on that path, but a further ~508 ms unexplained gap means the translation to cycle is partial, not 1:1 |

## Acceptance note on S1 (added 2026-09-20 after S3b)

S3b closed the reader-disagreement lead: only 2 of ~993 builds in the
13659302 log dropped any candidate to `nonceHigh`, and both were the delta
bug's own cascade -- the block whose 17,036 credited accounts were emptied,
and the next block, whose senders then read nonce 0. The fix removes the
cascade, so 35zzt's 37% occupancy is a genuine supply shortage and S1 stands.

The agent that analyses 35zzx attempt 2 must therefore also report the
`parallel fill drops` nonceHigh total across the B legs. It should be
approximately zero. If mass drops reappear on n42-r84, the occupancy gain is
not supply and prediction 77 must not be credited, whatever the B mean says.

## Closed

| id | step | outcome |
|----|------|---------|
| -- | sequential fallback deleted every credit-only recipient | fixed c0931aeb; cause of 35zzx attempt 1's BAD BLOCK; OPEN_ISSUES.md |
| -- | 35zzt: packet window 8 actually reaching the nodes | B 127.6k, standing best |
| -- | 35zzs: tx-bounded tail with a draining sealer | B 112.7k |
| -- | 35zzr: the fold outside the write transaction | B 89.1k -> 102.0k |

## Decision: the consensus safety journal stays where it is (2026-09-21, commander, delegated by the user)

No separate WAL and no second MDBX environment for the HotStuff safety state. The ConsensusState
record is written standalone by JournalVote and atomically with canonical chain data by
CommitToCanonicalWith's hook; two copies reopen the equivocation window persistence.go:68-75 warns
about, and a monotone max-merge on recovery would be new safety-critical code bought for no measured
gain: 35zzzd/35zzze show follower jcvMs = 0 and the leader's 334 ms journal wait disappears entirely
with a scheduling change (N42_LEADER_WRITE_AFTER_JOURNAL=1) that touches no safety semantics.
Revisit only if a later round measures journal waits again (watch jpvMs/jcvMs after S23 lets
write(v) overlap view v+1); the first remedy is again scheduling, not a second store.

## Box sharing (2026-09-21 19:10 EDT)

gov5 ran back-to-back rounds all day with ~3-minute gaps, shorter than the n42-rs runner's quiet check, so the
Rust fleet never got the box. After round 35zzzj ends gov5 stands down: no chain script queued, claim files
verified gone, fleet down for at least 100 minutes or until `.box-claim-rust` has come and gone, whichever is
later. From now on: at least 10 minutes of free box between gov5 rounds, and no chain script queued behind a
running round while n42-rs has work waiting. Prepared and waiting for the box: 35zzzk (S31, n42-r95).

## Position and path (commander, 2026-09-23 19:55 EDT)

Goal unchanged: the seven-node fleet's B mean. Lineage: safety first -- n42-r95 (S26 vote rule + S31 header vote) is the base, r97 adds the leader-side guard (35zzzm running). Score on this lineage: 117.9k (35zzzk, 16M sender cache) -> 35zzzn's B2 leg at 4M ran 135.7k / 121.6k, so a whole round at 4M should read ~128-130k; 35zzzm is that round. The pre-safety 132.8k (35zzzc) is no longer a comparable number.
What sets the score now, in order of measured size: (1) win2 is the collector (GC 20% -> 54% of CPU as the live heap climbs against GOMEMLIMIT 10GiB; the box cannot afford more memory): shrink the live heap -- sender cache done (S35, -0.87 GB), pool caps (S36a, queued), block cache (S36b), tail index and QMDB index (code knobs, later); (2) win1 sits at the eight generators' supply ceiling (~140k): raise supply only once (1) and (3) give the cycle room, or the extra ingest just feeds the collector; (3) the in-tenure cycle is the leader's own build (611-650 ms: parallel exec ~340, assemble/root ~147, pick ~61, ~80 unplaced -> S37 stamps it), and 25-46% of full blocks pay a ~1000 ms hand-over cycle because the next leader cannot speculate (S38 spec). Rounds alternate with n42-rs; each gov5 round leaves 10 free minutes.

## Honest position (commander, 2026-09-24 11:50 EDT)

The scored B mean has not moved: 127.6k (35zzt) -> 132.8k (35zzzc, pre-safety) -> 124.3k (35zzzm, safe lineage).
Why: since 35zzzb every win1 has sat at the eight generators' supply ceiling (~140k) and every cycle gain
(gossip fallback off, journal ordering, header vote, tenure 8) turned into LOWER OCCUPANCY, not higher TPS --
tenure 8 runs 0.87 s per full block (an engine ceiling of ~187k) with 34% full blocks. The engine got faster by
~38% per full block since 35zzza; the score cannot show it. Win2 was GC-bound and is now supply-bound too.
Decision: supply first, everything else waits. 35zzzu (16 x 500 senders) runs before 35zzzt. If sixteen
generators do not lift the aggregate submit rate, the next levers are generator-side (per-generator rate,
more funded senders, a second generator host) -- not the node. Each round now reports the ENGINE ceiling
(txs per full block / full-block cycle) next to the B mean so progress on the node is visible even while the
score is supply-capped.

